---
phase: 04-reports-templates
plan: 03
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified:
  - src/app/(protected)/assessment/results/page.tsx
  - src/components/reports/DownloadSection.tsx
  - src/components/reports/TemplateList.tsx
  - src/app/(protected)/dashboard/page.tsx
autonomous: true

must_haves:
  truths:
    - "User can download PDF report from results page with one click"
    - "User can browse and download individual governance policy templates"
    - "Results page shows clear risk level with Low/Medium/High/Critical label"
    - "Dashboard shows download actions for completed assessments"
    - "Download buttons show loading state during generation"
  artifacts:
    - path: "src/components/reports/DownloadSection.tsx"
      provides: "PDF and template download UI"
      exports: ["DownloadSection"]
    - path: "src/components/reports/TemplateList.tsx"
      provides: "Template selection and download UI"
      exports: ["TemplateList"]
  key_links:
    - from: "src/components/reports/DownloadSection.tsx"
      to: "/api/reports/[id]/pdf"
      via: "fetch for PDF download"
      pattern: "fetch.*api/reports.*pdf"
    - from: "src/components/reports/TemplateList.tsx"
      to: "/api/templates/[id]"
      via: "fetch for template download"
      pattern: "fetch.*api/templates"
    - from: "src/app/(protected)/assessment/results/page.tsx"
      to: "src/components/reports/DownloadSection.tsx"
      via: "component import"
      pattern: "import.*DownloadSection"
---

<objective>
Enhance results page and dashboard with PDF download, template browsing, and clear risk level display.

Purpose: Users need immediate access to all deliverables after assessment completion — the report and templates are the primary value proposition.
Output: Enhanced results page with download section, template list component, and dashboard integration.
</objective>

<execution_context>
@/Users/bwoodtalton/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bwoodtalton/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/(protected)/assessment/results/page.tsx
@src/app/(protected)/dashboard/page.tsx
@src/components/assessment/ScoreDisplay.tsx
@src/components/ui/button.tsx
@src/components/ui/card.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create download components for PDF and templates</name>
  <files>
    src/components/reports/DownloadSection.tsx
    src/components/reports/TemplateList.tsx
  </files>
  <action>
    1. Create `src/components/reports/DownloadSection.tsx`:
       - Props: assessmentId (string)
       - "use client" component
       - "Download Your Report" section header
       - Primary button: "Download PDF Report" with FileDown icon from lucide-react
       - onClick: fetch `/api/reports/${assessmentId}/pdf`, create blob URL, trigger download via hidden anchor element, revoke blob URL after
       - Loading state: Button shows Loader2 spinner + "Generating Report..." text, disabled during fetch
       - Error state: Show toast via react-hot-toast on failure
       - Brief description text: "Professional PDF report with executive summary, score breakdown, and recommendations"

    2. Create `src/components/reports/TemplateList.tsx`:
       - Props: assessmentId (string)
       - "use client" component
       - "Governance Policy Templates" section header
       - Hardcode template list from TEMPLATE_REGISTRY type (import from templates/types.ts)
       - Display as grid of Cards (2 columns on lg, 1 on mobile)
       - Each card shows: template name, brief description, category badge
       - Download button per template: "Download .docx" with FileText icon
       - onClick per template: fetch `/api/templates/${assessmentId}?template=${templateId}`, blob download same pattern as PDF
       - Per-template loading state (track which template is currently downloading)
       - "Download All Templates" button at top that sequentially downloads all 7 (with small delay between to avoid overwhelming browser)
       - Use shadcn Card, Button, Badge components consistent with existing design

    Download helper pattern (reuse in both components):
    ```
    const blob = await response.blob();
    const url = URL.createObjectURL(blob);
    const a = document.createElement('a');
    a.href = url;
    a.download = filename;
    a.click();
    URL.revokeObjectURL(url);
    ```

    Style using existing project patterns: rounded-[1.25rem] borders, section-divider classes, editorial-kicker for labels, bg-background/55 for nested cards.
  </action>
  <verify>
    Run `npx tsc --noEmit` to verify no TypeScript errors.
  </verify>
  <done>
    DownloadSection and TemplateList components exist with loading states, error handling, and blob download pattern. Styled consistently with existing design system.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate downloads into results page and dashboard</name>
  <files>
    src/app/(protected)/assessment/results/page.tsx
    src/app/(protected)/dashboard/page.tsx
  </files>
  <action>
    1. Update `src/app/(protected)/assessment/results/page.tsx`:
       - Import DownloadSection and TemplateList components
       - Add new section AFTER the existing RiskDrivers/ActionPlan grid and BEFORE the footer navigation:
         ```
         <div className="grid gap-6 xl:grid-cols-2">
           <Card>
             <CardContent className="pt-8">
               <DownloadSection assessmentId={assessmentId} />
             </CardContent>
           </Card>
           <Card>
             <CardContent className="pt-8">
               <TemplateList assessmentId={assessmentId} />
             </CardContent>
           </Card>
         </div>
         ```
       - Ensure assessmentId is passed from the store (already available via useAssessmentStore)

    2. Update `src/app/(protected)/dashboard/page.tsx`:
       - In the completed assessment card section (where "View Results" button exists), add two additional buttons:
         - "Download Report" button (outline variant) that links to `/api/reports/${assessment.id}/pdf` as direct download link (use `<a>` tag with download attribute, not client-side fetch, since dashboard is a server component)
         - "Get Templates" button (outline variant) that links to `/assessment/results` (route user to results page where full template selection is available)
       - Update the grid from `sm:grid-cols-2` to `sm:grid-cols-2 lg:grid-cols-4` to accommodate new buttons
       - Keep existing "View Results" and "Start New" buttons

    Maintain existing page structure — only ADD new sections, do not reorganize existing content.
  </action>
  <verify>
    Run `npx tsc --noEmit` to verify no TypeScript errors. Verify results page imports DownloadSection and TemplateList. Verify dashboard has download links for completed assessments.
  </verify>
  <done>
    Results page shows download section for PDF report and template list below existing score/risk content. Dashboard completed assessment cards include download actions. All download flows handle loading and error states.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- Results page renders DownloadSection and TemplateList components
- Dashboard shows download links for completed assessments
- PDF download triggers blob download with correct filename
- Template download triggers blob download with correct .docx filename
- Loading spinners display during generation
- Error toasts display on failure
</verification>

<success_criteria>
Users can download PDF report and Word templates immediately from the results page. Dashboard provides quick access to downloads for completed assessments. All download interactions include loading feedback and error handling.
</success_criteria>

<output>
After completion, create `.planning/phases/04-reports-templates/04-03-SUMMARY.md`
</output>
