---
phase: 11-dashboard-foundation
plan: 03
type: execute
wave: 3
depends_on: ["11-02"]
files_modified:
  - src/app/(protected)/advisor/page.tsx
  - src/components/layout/ProtectedNav.tsx
  - src/app/(protected)/advisor/dashboard/page.tsx
autonomous: false

must_haves:
  truths:
    - "Advisor can navigate to governance dashboard from the advisor portal and main navigation"
    - "Dashboard page loads within 2 seconds with Suspense streaming"
    - "Advisor cannot access any client data they are not authorized to see"
  artifacts:
    - path: "src/app/(protected)/advisor/page.tsx"
      provides: "Updated advisor portal with link to governance dashboard"
    - path: "src/components/layout/ProtectedNav.tsx"
      provides: "Updated navigation with governance dashboard link for advisors"
  key_links:
    - from: "src/app/(protected)/advisor/page.tsx"
      to: "/advisor/dashboard"
      via: "Link component navigation"
      pattern: "href.*advisor/dashboard"
    - from: "src/app/(protected)/advisor/dashboard/page.tsx"
      to: "Suspense"
      via: "React Suspense boundary wrapping data-dependent sections"
      pattern: "Suspense"
---

<objective>
Add navigation to the governance dashboard, optimize page loading with Suspense boundaries for sub-2-second performance, and verify the complete dashboard experience.

Purpose: Connects the dashboard to the existing advisor flow and ensures the 2-second performance requirement (DASH-05) is met.
Output: Navigable, performant governance dashboard verified by human.
</objective>

<execution_context>
@/Users/bwoodtalton/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bwoodtalton/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-dashboard-foundation/11-01-SUMMARY.md
@.planning/phases/11-dashboard-foundation/11-02-SUMMARY.md
@src/app/(protected)/advisor/page.tsx
@src/components/layout/ProtectedNav.tsx
@src/app/(protected)/advisor/dashboard/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add navigation and Suspense optimization</name>
  <files>src/app/(protected)/advisor/page.tsx, src/components/layout/ProtectedNav.tsx, src/app/(protected)/advisor/dashboard/page.tsx</files>
  <action>
    **Update ProtectedNav.tsx:**
    Read the existing component to understand the current navigation structure. Add a "Governance" or "Dashboard" nav link that points to `/advisor/dashboard`. This link should only appear when the `showAdvisor` prop is true (same condition as the existing Advisor Portal link). Match the existing nav link styling.

    **Update advisor portal page.tsx:**
    Add a prominent "Governance Dashboard" card or button near the top of the advisor portal page (after the header, before or alongside the summary metrics). Use a Card with a brief description like "View governance scores and risk levels across your client portfolio" and a Button linking to `/advisor/dashboard`. Style consistently with the existing hero-surface pattern.

    **Optimize dashboard page with Suspense:**
    Refactor `src/app/(protected)/advisor/dashboard/page.tsx` to use React Suspense for streaming:

    1. Extract the data-fetching and rendering into an async Server Component (can be in the same file as a separate component, e.g., `DashboardContent`).
    2. Wrap it in `<Suspense fallback={<DashboardSkeleton />}>` where `DashboardSkeleton` is imported from the loading.tsx skeleton components (or inline a similar skeleton).
    3. The outer page component renders immediately with the hero section (static content), then streams in the data-dependent sections.

    This ensures the page shell renders instantly while data loads in the background, meeting the 2-second perceived load time requirement.

    Do NOT add Redis caching at this stage. Prisma queries with proper indexes (already exist on `advisorId` and `clientId`) should be fast enough for 50 clients. Caching is a later optimization if needed.
  </action>
  <verify>
    `npx tsc --noEmit` passes.
    Grep for `Suspense` in dashboard page.tsx confirms streaming is implemented.
    Grep for `advisor/dashboard` in ProtectedNav.tsx confirms navigation link exists.
    Grep for `advisor/dashboard` in advisor page.tsx confirms portal link exists.
  </verify>
  <done>
    Navigation links to governance dashboard present in advisor portal and main nav.
    Dashboard page uses Suspense for streaming data load.
    Page shell renders immediately while data streams in.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete governance dashboard experience</name>
  <action>
    Human verifies the complete advisor governance dashboard at `/advisor/dashboard`.
    This checkpoint covers all DASH-01 through DASH-05 requirements.
  </action>
  <what-built>
    Complete advisor governance dashboard at `/advisor/dashboard` with:
    - Summary metrics (total families, average score, high risk count, assessed count)
    - Sortable data table with family names, governance scores, risk levels
    - Responsive layout (desktop full table, tablet condensed)
    - Loading skeleton during data fetch
    - Navigation from advisor portal and main nav
    - Row-level security (only assigned clients visible)
  </what-built>
  <how-to-verify>
    1. Start dev server: `npm run dev`
    2. Sign in as an advisor account
    3. Navigate to `/advisor/dashboard` (or use the new nav link)
    4. Verify: Dashboard loads with metrics cards at top and client table below
    5. Verify: Table shows Family Name, Score, Risk Level, Assessments, Last Assessment columns
    6. Verify: Click column headers to sort (score, name, risk level)
    7. Verify: Resize browser to tablet width (~768px) — confirm layout adjusts and remains usable
    8. Verify: Loading skeleton appears briefly during navigation (refresh the page to see it)
    9. Verify: If signed in as a different advisor, only that advisor's clients appear (data isolation)
  </how-to-verify>
  <verify>Human confirms dashboard meets all DASH requirements.</verify>
  <done>All Phase 11 requirements verified by human inspection.</done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- Advisor can navigate to `/advisor/dashboard` from advisor portal and main nav
- Dashboard renders metrics and sortable table with governance scores
- Table sorting works on all sortable columns
- Responsive layout adapts for desktop and tablet
- Loading skeleton appears during data fetch
- Only assigned clients are visible (row-level security via ClientAdvisorAssignment)
- Page perceived load time is under 2 seconds (Suspense streaming)
</verification>

<success_criteria>
- DASH-01: Advisor sees portfolio dashboard with assigned families and governance scores
- DASH-02: Multi-client dashboard with data isolation via advisor-scoped queries
- DASH-03: Responsive interface works on desktop and tablet
- DASH-04: Row-level security enforced through ClientAdvisorAssignment filtering
- DASH-05: Dashboard loads within 2 seconds using Suspense streaming and efficient queries
- Human verification confirms all of the above
</success_criteria>

<output>
After completion, create `.planning/phases/11-dashboard-foundation/11-03-SUMMARY.md`
</output>
