---
phase: 06-assessment-integration
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/assessment/types.ts
  - src/lib/assessment/personalization.ts
  - src/lib/assessment/branching.ts
  - src/lib/assessment/questions.ts
  - src/lib/assessment/personalization.test.ts
  - src/lib/assessment/branching.test.ts
autonomous: true

must_haves:
  truths:
    - "Questions with textTemplate render personalized text when profile data exists"
    - "Questions with textTemplate fall back to static text when profile is null"
    - "Profile-aware branching hides/shows questions based on household composition"
    - "Existing branching rules continue to work unchanged when no profile provided"
    - "Scoring algorithm produces identical results regardless of profile presence"
  artifacts:
    - path: "src/lib/assessment/types.ts"
      provides: "Extended Question interface with textTemplate and profileCondition"
      contains: "textTemplate"
    - path: "src/lib/assessment/personalization.ts"
      provides: "Dynamic text generation from household profile data"
      exports: ["getPersonalizedText", "HouseholdProfile"]
    - path: "src/lib/assessment/personalization.test.ts"
      provides: "Test coverage for personalization engine"
      min_lines: 50
    - path: "src/lib/assessment/branching.ts"
      provides: "Profile-aware shouldShowQuestion overload"
      contains: "HouseholdProfile"
    - path: "src/lib/assessment/branching.test.ts"
      provides: "Test coverage for profile-aware branching"
      contains: "profile"
  key_links:
    - from: "src/lib/assessment/personalization.ts"
      to: "src/lib/assessment/types.ts"
      via: "imports Question and HouseholdProfile types"
      pattern: "import.*types"
    - from: "src/lib/assessment/branching.ts"
      to: "src/lib/assessment/personalization.ts"
      via: "imports HouseholdProfile type"
      pattern: "HouseholdProfile"
---

<objective>
Build the profile-aware personalization engine and extend branching logic to support household composition-based question filtering and dynamic text generation.

Purpose: Enable assessment questions to personalize based on household data while maintaining 100% backward compatibility for existing assessments without profiles (ASSESS-01, ASSESS-02, ASSESS-04, ASSESS-05).

Output: Tested personalization and branching modules ready for UI integration.
</objective>

<execution_context>
@/Users/bwoodtalton/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bwoodtalton/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-assessment-integration/06-RESEARCH.md
@.planning/phases/05-profile-foundation/05-01-SUMMARY.md
@src/lib/assessment/types.ts
@src/lib/assessment/branching.ts
@src/lib/assessment/scoring.ts
@src/lib/assessment/questions.ts
@src/lib/schemas/profile.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend types and build personalization engine with tests</name>
  <files>
    src/lib/assessment/types.ts
    src/lib/assessment/personalization.ts
    src/lib/assessment/personalization.test.ts
  </files>
  <action>
**1. Extend Question interface in types.ts:**
Add two optional fields to the existing `Question` interface:
- `textTemplate?: (profile: HouseholdProfile | null) => string` — function that returns personalized question text given profile data. When present AND profile is non-null, its return value replaces `text`. When profile is null, falls back to `text`.
- `profileCondition?: (profile: HouseholdProfile) => boolean` — function that determines if question should show based on profile composition. Only evaluated when profile exists. When profile is null, question shows normally (backward compat).

Do NOT modify any other existing fields or interfaces. These additions must be purely additive.

**2. Create HouseholdProfile type in personalization.ts:**
Define a `HouseholdProfile` interface that maps to data available from `getHouseholdMembers()` server action:
```typescript
interface HouseholdMemberProfile {
  id: string;
  fullName: string;
  age: number | null;
  relationship: string; // FamilyRelationship enum value
  governanceRoles: string[]; // GovernanceRole enum values
  isResident: boolean;
}

interface HouseholdProfile {
  members: HouseholdMemberProfile[];
}
```

Export these types.

**3. Create personalization functions:**

`getPersonalizedText(question: Question, profile: HouseholdProfile | null): string`
- If `question.textTemplate` exists and `profile` is non-null, call `question.textTemplate(profile)` and return result
- Otherwise return `question.text`

`getMembersByRole(profile: HouseholdProfile, role: string): HouseholdMemberProfile[]`
- Filter members by governance role (case-insensitive match)

`getMembersByRelationship(profile: HouseholdProfile, relationship: string): HouseholdMemberProfile[]`
- Filter members by family relationship

`hasMultipleGenerations(profile: HouseholdProfile): boolean`
- Returns true if members span at least 2 different relationship types that imply generational difference (e.g., has both CHILD/GRANDCHILD and PARENT/GRANDPARENT, or has CHILD and primary user)

`hasMinors(profile: HouseholdProfile): boolean`
- Returns true if any member has age < 18

`hasSuccessors(profile: HouseholdProfile): boolean`
- Returns true if any member has SUCCESSOR governance role

**4. Write tests in personalization.test.ts (RED then GREEN):**
Test cases:
- `getPersonalizedText` returns static text when profile is null
- `getPersonalizedText` returns static text when no textTemplate
- `getPersonalizedText` calls textTemplate with profile when both exist
- `getMembersByRole` finds members with SUCCESSOR role
- `getMembersByRole` returns empty array when no match
- `hasMultipleGenerations` returns true for family with child + parent
- `hasMultipleGenerations` returns false for single-generation family
- `hasMinors` returns true when member age < 18
- `hasMinors` returns false when all members >= 18 or age null
- `hasSuccessors` returns true/false based on governance roles
  </action>
  <verify>Run `npx vitest run src/lib/assessment/personalization.test.ts` — all tests pass. TypeScript compiles without errors: `npx tsc --noEmit`.</verify>
  <done>Personalization engine complete with HouseholdProfile type, text generation function, and helper utilities. All tests pass. No existing types or interfaces broken.</done>
</task>

<task type="auto">
  <name>Task 2: Extend branching logic with profile awareness and add profile-based question conditions</name>
  <files>
    src/lib/assessment/branching.ts
    src/lib/assessment/branching.test.ts
    src/lib/assessment/questions.ts
  </files>
  <action>
**1. Extend shouldShowQuestion in branching.ts:**
Create an overloaded version that accepts optional HouseholdProfile:

```typescript
export function shouldShowQuestion(
  question: Question,
  answers: Record<string, unknown>,
  profile?: HouseholdProfile | null
): boolean {
  // Existing answer-based branching (unchanged)
  if (question.branchingRule) {
    const { dependsOn, showIf } = question.branchingRule;
    const dependencyAnswer = answers[dependsOn];
    if (dependencyAnswer === undefined || dependencyAnswer === null) {
      return false;
    }
    if (!showIf(dependencyAnswer)) {
      return false;
    }
  }

  // NEW: Profile-based condition (only when profile exists)
  if (question.profileCondition && profile) {
    return question.profileCondition(profile);
  }

  // Default: show question (backward compat — no profile = show everything)
  return true;
}
```

The key backward compat guarantee: when `profile` is undefined/null, `profileCondition` is never evaluated, so all existing behavior preserved.

**2. Update all branching.ts functions that call shouldShowQuestion:**
Update `getVisibleQuestions`, `getNextQuestion`, `getPreviousQuestion`, `calculateCompletionPercentage`, `getUnansweredRequiredQuestions`, `detectBranchingChanges`, and `getOrphanedAnswerIds` to accept an optional `profile?: HouseholdProfile | null` parameter and pass it through to `shouldShowQuestion`. All parameters must remain optional with default undefined so callers without profile data work unchanged.

**3. Add profileCondition to select questions in questions.ts:**
Add `profileCondition` to questions where household composition matters. Target these specific scenarios:

- **Succession questions** (sub-category: succession-planning): Add `profileCondition: (p) => hasMultipleGenerations(p) || hasSuccessors(p)` — only show succession questions when family has multiple generations or designated successors. Import `hasMultipleGenerations` and `hasSuccessors` from personalization.ts.

- **Trust questions** (teg-02 through teg-08, already gated by teg-01 answer): Add `profileCondition: (p) => p.members.some(m => m.governanceRoles.includes('TRUSTEE'))` to teg-03 (trustee selection question) — only show trustee-specific questions when household has trustees.

- Add 2-3 `textTemplate` examples to existing questions:
  - On a succession question: `textTemplate: (p) => { const successor = getMembersByRole(p, 'SUCCESSOR')[0]; return successor ? \`How prepared is ${successor.fullName} for leadership responsibility?\` : 'How prepared is your primary successor for leadership responsibility?'; }`
  - On a decision-making question: `textTemplate: (p) => { const dm = getMembersByRole(p, 'DECISION_MAKER')[0]; return dm ? \`How does ${dm.fullName} communicate major financial decisions to the family?\` : 'How does the primary decision maker communicate major financial decisions to the family?'; }`

Import necessary functions from personalization.ts at the top of questions.ts.

**4. Update existing branching tests and add profile-aware tests:**
In branching.test.ts, add test cases:
- `shouldShowQuestion` with profile=undefined behaves identically to before (backward compat)
- `shouldShowQuestion` with profile=null behaves identically to before
- `shouldShowQuestion` hides question when profileCondition returns false
- `shouldShowQuestion` shows question when profileCondition returns true
- `shouldShowQuestion` evaluates both branchingRule AND profileCondition (both must pass)
- `getVisibleQuestions` with profile filters correctly
- `getVisibleQuestions` without profile returns same results as before

Ensure all existing tests in branching.test.ts continue to pass unchanged.
  </action>
  <verify>Run `npx vitest run src/lib/assessment/branching.test.ts` — all tests pass (existing + new). Run `npx vitest run src/lib/assessment/personalization.test.ts` — still passes. Run `npx tsc --noEmit` — no type errors. Run `npx vitest run` — full test suite passes.</verify>
  <done>Branching logic extended with profile awareness. Select questions have profileCondition and textTemplate. All existing branching behavior preserved when profile is not provided. Full test suite green.</done>
</task>

</tasks>

<verification>
1. `npx vitest run` — all tests pass
2. `npx tsc --noEmit` — no type errors
3. Scoring engine unchanged — `calculatePillarScore` signature and behavior identical
4. Existing `shouldShowQuestion(question, answers)` calls work without modification
5. Questions without profileCondition/textTemplate behave exactly as before
</verification>

<success_criteria>
- Personalization engine generates dynamic text from household data
- Branching logic filters questions by household composition
- All existing tests pass unmodified
- No changes to scoring algorithm
- HouseholdProfile type ready for UI layer consumption
</success_criteria>

<output>
After completion, create `.planning/phases/06-assessment-integration/06-01-SUMMARY.md`
</output>
