---
phase: 20-identity-risk-intelligence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/identity-risk/types.ts
  - src/lib/identity-risk/questions.ts
  - src/lib/identity-risk/scoring.ts
autonomous: true
must_haves:
  truths:
    - "Identity risk question bank defines questions covering social media exposure, public information visibility, digital footprint, and family visibility"
    - "Identity risk scoring wrapper delegates to proven calculatePillarScore engine"
    - "All questions produce values on 0-10 scale matching existing governance and cyber risk patterns"
  artifacts:
    - path: "src/lib/identity-risk/types.ts"
      provides: "Identity risk pillar ID, subcategory constants, and re-exported assessment types"
      contains: "IDENTITY_PILLAR_ID"
    - path: "src/lib/identity-risk/questions.ts"
      provides: "Complete question bank with pillar definition and subcategories"
      exports: ["identityRiskPillar", "identityRiskQuestions", "allIdentityQuestions"]
    - path: "src/lib/identity-risk/scoring.ts"
      provides: "Thin scoring wrapper delegating to calculatePillarScore"
      exports: ["calculateIdentityRiskScore"]
  key_links:
    - from: "src/lib/identity-risk/scoring.ts"
      to: "src/lib/assessment/scoring.ts"
      via: "import calculatePillarScore"
      pattern: "calculatePillarScore"
    - from: "src/lib/identity-risk/questions.ts"
      to: "src/lib/identity-risk/types.ts"
      via: "import IDENTITY_PILLAR_ID, IDENTITY_SUBCATEGORIES"
      pattern: "IDENTITY_PILLAR_ID"
---

<objective>
Create the identity risk domain layer with question bank, type definitions, and scoring wrapper.

Purpose: Establish the data foundation for identity risk assessment as a third pillar alongside governance and cyber risk, following the proven pattern from Phase 19.
Output: Three domain files that define the complete identity risk assessment structure.
</objective>

<execution_context>
@/Users/bwoodtalton/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bwoodtalton/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-identity-risk-intelligence/20-RESEARCH.md
@src/lib/cyber-risk/types.ts
@src/lib/cyber-risk/questions.ts
@src/lib/cyber-risk/scoring.ts
@src/lib/assessment/types.ts
@src/lib/assessment/scoring.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Identity risk types and question bank</name>
  <files>src/lib/identity-risk/types.ts, src/lib/identity-risk/questions.ts</files>
  <action>
Create `src/lib/identity-risk/types.ts` following the exact pattern of `src/lib/cyber-risk/types.ts`:
- Re-export core types from `../assessment/types` (Question, Pillar, SubCategory, ScoreResult, RiskLevel, QuestionType, QuestionOption, BranchingRule, MissingControl, CategoryScore)
- Export `IDENTITY_PILLAR_ID = 'identity-risk'`
- Export `IDENTITY_SUBCATEGORIES` const object with four subcategories:
  - `SOCIAL_EXPOSURE: 'social-exposure'` (weight 4 - primary focus per IDENTITY-01)
  - `PUBLIC_INFORMATION: 'public-information'` (weight 4 - primary focus per IDENTITY-02)
  - `DIGITAL_FOOTPRINT: 'digital-footprint'` (weight 3)
  - `FAMILY_VISIBILITY: 'family-visibility'` (weight 3)

Create `src/lib/identity-risk/questions.ts` following the exact pattern of `src/lib/cyber-risk/questions.ts`:
- Import Question, Pillar from `../assessment/types`
- Import IDENTITY_PILLAR_ID, IDENTITY_SUBCATEGORIES from `./types`
- Define 20-24 questions across four subcategories:

**Social Exposure (5-6 questions, weight 4):**
- Social media platform count/presence
- Privacy settings review frequency
- Personal information shared on profiles (real name, location, employer)
- Public vs private account settings
- Who can see posts/followers
- Social media activity frequency

**Public Information (5-6 questions, weight 4):**
- Property records visibility
- Business registration exposure
- Court/legal filing presence
- Data broker listing awareness
- People-search site presence
- Professional directory listings

**Digital Footprint (5-6 questions, weight 3):**
- Email address exposure across services
- Username reuse across platforms
- Old/unused account cleanup
- Personal website/blog presence
- Forum/community participation
- Online shopping account proliferation

**Family Visibility (4-5 questions, weight 3):**
- Family member social media tagging practices
- Children's online presence management
- Family event/location sharing patterns
- Household information shared publicly
- Family photo sharing practices

All questions must:
- Use `type: 'maturity-scale'` or `type: 'single-choice'`
- Have `scoreMap` producing values in 0-10 range
- Set `pillar: IDENTITY_PILLAR_ID`
- Set appropriate `subCategory` from IDENTITY_SUBCATEGORIES
- Have `required: true`
- Include `helpText` explaining why this matters for identity risk
- Use question IDs prefixed with `identity-` (e.g., `identity-se-01`)

Include at least one branching rule (e.g., if "no social media accounts" then skip detailed privacy settings question).

Export `identityRiskPillar` (Pillar object), `identityRiskQuestions` (flat Question[]), and `allIdentityQuestions` (alias for identityRiskQuestions).

Use relative imports (not path aliases) within the identity-risk module, matching the cyber-risk pattern.
  </action>
  <verify>Run `npx tsc --noEmit` to confirm all files compile without errors. Verify question count is 20-24. Verify all scoreMap values are in 0-10 range.</verify>
  <done>Identity risk types and 20-24 question bank exist with four subcategories, proper scoreMap ranges, and branching logic. TypeScript compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Identity risk scoring wrapper</name>
  <files>src/lib/identity-risk/scoring.ts</files>
  <action>
Create `src/lib/identity-risk/scoring.ts` following the exact pattern of `src/lib/cyber-risk/scoring.ts`:
- Import `calculatePillarScore` from `../assessment/scoring`
- Import `identityRiskPillar, identityRiskQuestions` from `./questions`
- Import `ScoreResult` from `../assessment/types`
- Export `calculateIdentityRiskScore(answers: Record<string, unknown>, visibleQuestionIds?: string[]): ScoreResult`
- Function body: single delegation to `calculatePillarScore(answers, identityRiskPillar, identityRiskQuestions, visibleQuestionIds)`
- Include JSDoc matching the cyber-risk scoring wrapper style

Zero scoring logic duplication. Thin wrapper only.
  </action>
  <verify>Run `npx tsc --noEmit` to confirm compilation. Verify the function signature matches calculateCyberRiskScore pattern.</verify>
  <done>Identity risk scoring wrapper exists, delegates to calculatePillarScore, compiles without errors.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- `src/lib/identity-risk/types.ts` exports IDENTITY_PILLAR_ID and IDENTITY_SUBCATEGORIES
- `src/lib/identity-risk/questions.ts` exports identityRiskPillar, identityRiskQuestions, allIdentityQuestions
- `src/lib/identity-risk/scoring.ts` exports calculateIdentityRiskScore
- Question count is 20-24 across 4 subcategories
- All scoreMaps produce values in 0-10 range
</verification>

<success_criteria>
Identity risk domain layer exists with complete type system, 20-24 question bank covering social exposure, public information, digital footprint, and family visibility, plus thin scoring wrapper. All files compile and follow established cyber-risk patterns exactly.
</success_criteria>

<output>
After completion, create `.planning/phases/20-identity-risk-intelligence/20-01-SUMMARY.md`
</output>
