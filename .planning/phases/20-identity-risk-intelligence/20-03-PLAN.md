---
phase: 20-identity-risk-intelligence
plan: 03
type: execute
wave: 3
depends_on: ["20-02"]
files_modified:
  - src/lib/identity-risk/recommendations.ts
  - src/app/api/identity-risk/recommendations/route.ts
  - src/app/(protected)/advisor/identity-risk/page.tsx
  - src/app/(protected)/advisor/identity-risk/loading.tsx
  - src/lib/actions/advisor-actions.ts
  - src/app/(protected)/advisor/page.tsx
autonomous: true
must_haves:
  truths:
    - "System generates AI-powered identity risk recommendations from assessment results"
    - "Fallback recommendations work without OpenAI API key"
    - "Advisor can view client identity risk scores in dedicated portal section"
    - "Advisor portal navigation includes Identity Risk link"
  artifacts:
    - path: "src/lib/identity-risk/recommendations.ts"
      provides: "AI recommendation engine for identity risk with fallbacks"
      exports: ["generateIdentityRecommendations"]
    - path: "src/app/api/identity-risk/recommendations/route.ts"
      provides: "POST/GET endpoints for identity risk recommendations"
      exports: ["GET", "POST"]
    - path: "src/app/(protected)/advisor/identity-risk/page.tsx"
      provides: "Advisor dashboard for client identity risk oversight"
      contains: "getIdentityRiskDashboardData"
    - path: "src/app/(protected)/advisor/identity-risk/loading.tsx"
      provides: "Loading skeleton for identity risk dashboard"
  key_links:
    - from: "src/lib/identity-risk/recommendations.ts"
      to: "src/lib/openai.ts"
      via: "import getOpenAIClient"
      pattern: "getOpenAIClient"
    - from: "src/app/api/identity-risk/recommendations/route.ts"
      to: "src/lib/identity-risk/recommendations.ts"
      via: "import generateIdentityRecommendations"
      pattern: "generateIdentityRecommendations"
    - from: "src/app/(protected)/advisor/page.tsx"
      to: "src/app/(protected)/advisor/identity-risk/page.tsx"
      via: "navigation link"
      pattern: "identity-risk"
    - from: "src/lib/actions/advisor-actions.ts"
      to: "prisma.pillarScore"
      via: "database query filtered by identity-risk pillar"
      pattern: "identity-risk"
---

<objective>
Create AI-powered identity risk recommendations and dedicated advisor portal section for client identity risk oversight.

Purpose: Complete IDENTITY-01 and IDENTITY-02 by adding actionable recommendations and advisor visibility for identity risk assessments.
Output: Recommendation engine, API endpoints, and advisor dashboard section for identity risk.
</objective>

<execution_context>
@/Users/bwoodtalton/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bwoodtalton/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-identity-risk-intelligence/20-02-SUMMARY.md
@.planning/phases/19-cyber-risk-foundation/19-03-SUMMARY.md
@src/lib/cyber-risk/recommendations.ts
@src/app/api/cyber-risk/recommendations/route.ts
@src/app/(protected)/advisor/cyber-risk/page.tsx
@src/app/(protected)/advisor/cyber-risk/loading.tsx
@src/lib/actions/advisor-actions.ts
@src/app/(protected)/advisor/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: AI identity risk recommendation engine and API</name>
  <files>src/lib/identity-risk/recommendations.ts, src/app/api/identity-risk/recommendations/route.ts</files>
  <action>
Create `src/lib/identity-risk/recommendations.ts` following the exact pattern of `src/lib/cyber-risk/recommendations.ts`:

1. Export `IdentityRecommendation` interface (same shape as CyberRecommendation: title, description, priority, category)
2. Export `generateIdentityRecommendations(scoreResult: ScoreResult, answers: Record<string, unknown>): Promise<IdentityRecommendation[]>`
3. System prompt should position AI as an "identity protection and privacy advisor for high-net-worth families" focusing on:
   - Social media privacy hardening
   - Public information removal (data broker opt-outs)
   - Digital footprint reduction
   - Family visibility management
4. Build structured prompt from identity risk scores and missing controls
5. Fallback static recommendations mapped by missing control types:
   - social-exposure: "Review and restrict social media privacy settings across all platforms"
   - public-information: "Submit opt-out requests to major data broker sites (Spokeo, Whitepages, BeenVerified)"
   - digital-footprint: "Audit and close unused online accounts, use unique email aliases"
   - family-visibility: "Establish family social media guidelines for tagging and location sharing"
6. Use gpt-4o-mini model (same as cyber risk, per established decision)

Create `src/app/api/identity-risk/recommendations/route.ts` following the exact pattern of `src/app/api/cyber-risk/recommendations/route.ts`:
- POST: Generate recommendations from assessment data, cache in PillarScore.missingControls
- GET: Retrieve cached recommendations
- Full authentication and ownership verification
- Filter by pillar = 'identity-risk'
  </action>
  <verify>Run `npx tsc --noEmit`. Verify both files compile. Verify fallback recommendations cover all four subcategories.</verify>
  <done>Identity risk AI recommendations generate from assessment data with fallback system. API endpoints authenticate and cache results.</done>
</task>

<task type="auto">
  <name>Task 2: Advisor identity risk dashboard</name>
  <files>src/app/(protected)/advisor/identity-risk/page.tsx, src/app/(protected)/advisor/identity-risk/loading.tsx, src/lib/actions/advisor-actions.ts, src/app/(protected)/advisor/page.tsx</files>
  <action>
Create advisor identity risk dashboard following the exact pattern of `src/app/(protected)/advisor/cyber-risk/`:

**Server action (`src/lib/actions/advisor-actions.ts`):**
1. Add `getIdentityRiskDashboardData` server action following `getCyberRiskDashboardData` pattern
2. Query PillarScore where pillar = 'identity-risk' for advisor's clients
3. Return portfolio metrics: total clients, assessed count, average score, at-risk count
4. Return client list with identity risk scores, risk levels, assessment dates
5. Apply same row-level security (advisor can only see their clients)

**Dashboard page (`src/app/(protected)/advisor/identity-risk/page.tsx`):**
1. Follow `cyber-risk/page.tsx` pattern exactly
2. Suspense-enabled dashboard with metrics cards and client table
3. Risk level sorting (critical -> high -> medium -> low -> unassessed)
4. Use UserSearch (Fingerprint) icon for identity risk visual distinction
5. Portfolio metrics: total clients, assessed count, average identity risk score, at-risk count
6. Client table columns: name, identity risk score, risk level, assessed date
7. Empty state when no clients have completed identity risk assessment

**Loading skeleton (`src/app/(protected)/advisor/identity-risk/loading.tsx`):**
1. Follow `cyber-risk/loading.tsx` pattern exactly

**Advisor portal navigation (`src/app/(protected)/advisor/page.tsx`):**
1. Add Identity Risk navigation card with Fingerprint icon (from lucide-react)
2. Place after Cyber Risk card in navigation
3. Route to `/advisor/identity-risk`
4. Description: "Monitor client identity exposure and privacy risk scores"
  </action>
  <verify>Run `npm run build` to verify all routes compile and generate. Verify advisor portal shows Identity Risk navigation card. Verify loading skeleton renders.</verify>
  <done>Advisor can navigate to /advisor/identity-risk, see client identity risk scores with portfolio metrics, and loading states work correctly.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds with zero errors
- Identity risk recommendations API returns recommendations (or fallbacks without API key)
- Advisor portal navigation includes Identity Risk link
- /advisor/identity-risk route renders with Suspense loading
- Client table displays identity risk scores from PillarScore data
- Existing cyber risk and governance advisor dashboards unaffected
</verification>

<success_criteria>
AI-powered identity risk recommendations generate from assessment results with graceful fallbacks. Advisors can view client identity risk scores in dedicated portal section with portfolio-level metrics. Full build passes without errors.
</success_criteria>

<output>
After completion, create `.planning/phases/20-identity-risk-intelligence/20-03-SUMMARY.md`
</output>
