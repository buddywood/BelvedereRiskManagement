---
phase: 20-identity-risk-intelligence
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - src/app/(protected)/assessment/page.tsx
  - src/app/(protected)/assessment/[pillarSlug]/[questionIndex]/page.tsx
  - src/app/api/assessment/[id]/score/route.ts
  - src/app/(protected)/assessment/results/page.tsx
autonomous: true
must_haves:
  truths:
    - "Family can start and complete identity risk assessment from assessment hub"
    - "Identity risk questions render at /assessment/identity-risk/[index] with proper navigation"
    - "Score API calculates and persists identity risk scores on 0-10 scale"
    - "Results page displays identity risk scores when assessment completed"
    - "Governance and cyber risk assessment flows remain unchanged"
  artifacts:
    - path: "src/app/(protected)/assessment/page.tsx"
      provides: "Third pillar card in assessment hub"
      contains: "identity-risk"
    - path: "src/app/api/assessment/[id]/score/route.ts"
      provides: "Identity risk scoring via getPillarConfig"
      contains: "identity-risk"
  key_links:
    - from: "src/app/(protected)/assessment/page.tsx"
      to: "src/lib/identity-risk/questions.ts"
      via: "import identityRiskPillar, allIdentityQuestions"
      pattern: "identity-risk/questions"
    - from: "src/app/api/assessment/[id]/score/route.ts"
      to: "src/lib/identity-risk/scoring.ts"
      via: "import calculateIdentityRiskScore in getPillarConfig"
      pattern: "calculateIdentityRiskScore"
    - from: "src/app/(protected)/assessment/[pillarSlug]/[questionIndex]/page.tsx"
      to: "src/lib/identity-risk/questions.ts"
      via: "getQuestionsForPillar helper"
      pattern: "identity-risk"
---

<objective>
Wire identity risk assessment into the existing multi-pillar UI, question flow, and scoring API.

Purpose: Enable families to complete identity risk assessments through the same UI patterns established for governance and cyber risk.
Output: Third pillar fully integrated into assessment hub, question navigation, scoring, and results display.
</objective>

<execution_context>
@/Users/bwoodtalton/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bwoodtalton/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-identity-risk-intelligence/20-01-SUMMARY.md
@.planning/phases/19-cyber-risk-foundation/19-02-SUMMARY.md
@src/app/(protected)/assessment/page.tsx
@src/app/(protected)/assessment/[pillarSlug]/[questionIndex]/page.tsx
@src/app/api/assessment/[id]/score/route.ts
@src/app/(protected)/assessment/results/page.tsx
@src/lib/hooks/useAssessmentNavigation.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add identity risk pillar to assessment hub</name>
  <files>src/app/(protected)/assessment/page.tsx</files>
  <action>
Extend the existing assessment hub page to include identity risk as a third pillar card:

1. Import identity risk pillar and questions:
   - Add `import { identityRiskPillar, allIdentityQuestions } from '@/lib/identity-risk/questions';`

2. Add identity risk entry to `ASSESSMENT_PILLARS` array:
   - Add third entry following the cyber-risk pattern
   - Use `identityRiskPillar` for the pillar object (id: 'identity-risk', name: 'Identity Risk', slug: 'identity-risk')
   - Set appropriate description about social media exposure and public information visibility
   - Set estimatedMinutes to ~20 (20-24 questions)

3. Update `OverallProgress` component:
   - Change `totalPillars` from 2 to 3

4. Ensure `getQuestionsForPillar` helper handles 'identity-risk' case:
   - Return `allIdentityQuestions` for identity-risk slug

No changes to governance or cyber risk pillar configurations. The existing multi-pillar architecture should handle this addition with minimal code changes since it already iterates over ASSESSMENT_PILLARS.
  </action>
  <verify>Run `npx tsc --noEmit`. Verify the ASSESSMENT_PILLARS array has 3 entries. Verify OverallProgress shows totalPillars: 3.</verify>
  <done>Assessment hub displays three pillar cards: Family Governance, Cyber Risk, and Identity Risk with independent progress tracking.</done>
</task>

<task type="auto">
  <name>Task 2: Enable identity risk question flow and scoring</name>
  <files>src/app/(protected)/assessment/[pillarSlug]/[questionIndex]/page.tsx, src/app/api/assessment/[id]/score/route.ts, src/app/(protected)/assessment/results/page.tsx</files>
  <action>
Wire identity risk into question navigation and scoring, following the exact pattern used when cyber-risk was added in Plan 19-02:

**Question page (`[pillarSlug]/[questionIndex]/page.tsx`):**
1. Import identity risk questions: `import { allIdentityQuestions } from '@/lib/identity-risk/questions';`
2. Extend `getQuestionsForPillar` helper to handle `'identity-risk'` case returning `allIdentityQuestions`
3. No other changes needed - the pillar-agnostic navigation already handles arbitrary pillar slugs

**Score API (`api/assessment/[id]/score/route.ts`):**
1. Import identity risk modules:
   - `import { identityRiskPillar, identityRiskQuestions } from '@/lib/identity-risk/questions';`
   - `import { calculateIdentityRiskScore } from '@/lib/identity-risk/scoring';`
2. Extend `getPillarConfig` switch to handle `'identity-risk'`:
   - Return `{ pillarData: identityRiskPillar, questions: identityRiskQuestions }`
3. No other changes - the existing pillar-agnostic score calculation handles it

**Results page (`assessment/results/page.tsx`):**
1. Ensure dynamic pillar detection from store.currentPillar works for identity-risk
2. The existing logic should already handle arbitrary pillar strings since 19-02 made it dynamic
3. Verify identity-risk specific features: hide governance-only downloads/templates (same as cyber-risk behavior)
4. If there's an explicit pillar check (e.g., only 'family-governance' and 'cyber-risk'), add 'identity-risk' to it

Apply customization logic only to family-governance pillar (identity-risk not customized, same as cyber-risk).
Maintain backwards compatibility by keeping default pillar as 'family-governance'.
  </action>
  <verify>Run `npx tsc --noEmit`. Run `npm run build` to verify all routes compile. Check that getPillarConfig handles all three pillars. Check getQuestionsForPillar handles all three pillars.</verify>
  <done>Identity risk questions render at /assessment/identity-risk/[index], score API calculates identity risk scores, results page displays identity risk results. Governance and cyber risk flows unchanged.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds with zero errors
- Assessment hub shows 3 pillar cards
- `getPillarConfig` handles 'family-governance', 'cyber-risk', and 'identity-risk'
- `getQuestionsForPillar` handles all three pillar slugs
- Results page adapts to identity-risk pillar
- No regressions in governance or cyber risk flows
</verification>

<success_criteria>
Families can start identity risk assessment from hub, complete all questions with proper navigation, receive scored results on 0-10 scale, and navigate back to complete remaining pillars. Existing governance and cyber risk flows unaffected.
</success_criteria>

<output>
After completion, create `.planning/phases/20-identity-risk-intelligence/20-02-SUMMARY.md`
</output>
