---
phase: 12-analytics-engine
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - src/app/(protected)/advisor/analytics/[clientId]/page.tsx
  - src/app/(protected)/advisor/analytics/[clientId]/loading.tsx
  - src/components/analytics/GovernanceTrendChart.tsx
  - src/components/analytics/CategoryBreakdownChart.tsx
  - src/components/analytics/TrendIndicator.tsx
  - src/components/analytics/AssessmentComparisonView.tsx
  - src/lib/actions/advisor-actions.ts
  - src/components/dashboard/GovernanceTable.tsx
autonomous: true

must_haves:
  truths:
    - "Advisor can view line chart showing governance score over time for a family"
    - "Advisor can see bar chart breaking down all 8 governance categories with scores"
    - "Advisor can see trend indicators showing improving/declining/stable between assessments"
    - "Advisor can compare category scores between assessment periods"
    - "Charts render correctly with governance data (8 categories, scores 0-10)"
    - "Advisor can navigate from dashboard table to family analytics page"
  artifacts:
    - path: "src/app/(protected)/advisor/analytics/[clientId]/page.tsx"
      provides: "Family analytics page with charts"
      contains: "Suspense"
    - path: "src/components/analytics/GovernanceTrendChart.tsx"
      provides: "Line chart for historical governance scores"
      contains: "LineChart"
    - path: "src/components/analytics/CategoryBreakdownChart.tsx"
      provides: "Bar chart for 8 governance category scores"
      contains: "BarChart"
    - path: "src/components/analytics/TrendIndicator.tsx"
      provides: "Visual trend direction badge"
      exports: ["TrendIndicator"]
    - path: "src/components/analytics/AssessmentComparisonView.tsx"
      provides: "Side-by-side assessment period comparison"
      exports: ["AssessmentComparisonView"]
  key_links:
    - from: "src/app/(protected)/advisor/analytics/[clientId]/page.tsx"
      to: "src/lib/actions/advisor-actions.ts"
      via: "server action call"
      pattern: "getFamilyAnalyticsData"
    - from: "src/components/dashboard/GovernanceTable.tsx"
      to: "/advisor/analytics/[clientId]"
      via: "Next.js Link in table row"
      pattern: "Link.*advisor/analytics"
    - from: "src/components/analytics/GovernanceTrendChart.tsx"
      to: "recharts"
      via: "LineChart component import"
      pattern: "import.*LineChart.*recharts"
---

<objective>
Build the family analytics page with governance trend line chart, category breakdown bar chart, trend indicators, and assessment period comparison. Wire navigation from the dashboard table.

Purpose: Deliver VIZ-01 through VIZ-05 requirements — advisors can analyze governance trends and risk patterns for individual families across time periods.
Output: Complete family analytics experience accessible from the governance dashboard.
</objective>

<execution_context>
@/Users/bwoodtalton/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bwoodtalton/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-analytics-engine/12-RESEARCH.md
@.planning/phases/12-analytics-engine/12-01-SUMMARY.md

@src/lib/analytics/types.ts
@src/lib/analytics/queries.ts
@src/lib/analytics/formatters.ts
@src/components/analytics/ChartContainer.tsx
@src/app/(protected)/advisor/dashboard/page.tsx
@src/components/dashboard/GovernanceTable.tsx
@src/lib/actions/advisor-actions.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create chart components and trend indicator</name>
  <files>src/components/analytics/GovernanceTrendChart.tsx, src/components/analytics/CategoryBreakdownChart.tsx, src/components/analytics/TrendIndicator.tsx, src/components/analytics/AssessmentComparisonView.tsx</files>
  <action>
    All chart components are "use client" components.

    1. Create `src/components/analytics/GovernanceTrendChart.tsx`:
       - Props: `{ data: GovernanceTrendPoint[] }` (import from analytics types)
       - Renders a Recharts `LineChart` inside a `ChartContainer` with title "Governance Score Trend"
       - XAxis: dataKey="date", tickFormatter using `formatChartDate` from formatters
       - YAxis: domain [0, 10], label "Governance Score"
       - Single Line: dataKey="overallScore", stroke using primary CHART_COLORS, strokeWidth 2, type "monotone", dot markers for each assessment point
       - Tooltip: show formatted date and score with 1 decimal
       - If data has fewer than 2 points, show a message "Complete more assessments to see trends" instead of the chart
       - Add aria-label for accessibility

    2. Create `src/components/analytics/CategoryBreakdownChart.tsx`:
       - Props: `{ data: CategoryBreakdownPoint[]; title?: string }` (default title "Governance Category Breakdown")
       - Renders a Recharts `BarChart` inside a `ChartContainer`
       - XAxis: dataKey="categoryName", angle -35, textAnchor "end", height 80, interval 0 (show all 8 labels)
       - YAxis: domain [0, 10], label "Score"
       - Single Bar: dataKey="score", fill using primary blue (#3b82f6)
       - Tooltip: show category name and score formatted to 1 decimal
       - Color-code bars by score: >= 7 green (#22c55e), >= 5 amber (#f59e0b), >= 3 orange (#f97316), < 3 red (#ef4444). Use Recharts Cell component to set individual bar colors.

    3. Create `src/components/analytics/TrendIndicator.tsx`:
       - Props: `{ direction: 'improving' | 'declining' | 'stable' | 'new'; scoreDelta?: number }`
       - Renders a small inline badge:
         - improving: green text, up arrow (Lucide TrendingUp), shows "+{delta}" if delta provided
         - declining: red text, down arrow (Lucide TrendingDown), shows "-{delta}" if delta provided
         - stable: muted text, minus icon (Lucide Minus), shows "No change"
         - new: blue text, shows "First assessment"
       - Use existing Badge component variant styling pattern from ScoreBadge.tsx

    4. Create `src/components/analytics/AssessmentComparisonView.tsx`:
       - Props: `{ assessments: AssessmentComparison[] }`
       - If fewer than 2 assessments, show "Complete another assessment to compare periods"
       - Renders the two most recent assessments side-by-side in a responsive grid (1 col mobile, 2 col desktop)
       - Each side shows: assessment date (formatted), overall score with TrendIndicator, and a small CategoryBreakdownChart (height 250)
       - Between the two columns (or below on mobile), show a summary: "Score changed from X.X to Y.Y" with appropriate TrendIndicator
  </action>
  <verify>
    Run `npx tsc --noEmit` — no type errors. Verify all 4 components import from recharts correctly. Verify TrendIndicator handles all 4 direction variants.
  </verify>
  <done>GovernanceTrendChart renders line chart with 0-10 scale, CategoryBreakdownChart renders color-coded bars for 8 categories, TrendIndicator shows direction badges, AssessmentComparisonView displays side-by-side period comparison.</done>
</task>

<task type="auto">
  <name>Task 2: Create analytics page, server action, and dashboard navigation</name>
  <files>src/app/(protected)/advisor/analytics/[clientId]/page.tsx, src/app/(protected)/advisor/analytics/[clientId]/loading.tsx, src/lib/actions/advisor-actions.ts, src/components/dashboard/GovernanceTable.tsx</files>
  <action>
    1. Add server action `getFamilyAnalyticsData(clientId: string)` to `src/lib/actions/advisor-actions.ts`:
       - Follow existing pattern: `requireAdvisorRole()`, `getAdvisorProfileOrThrow(userId)`
       - Call `getFamilyGovernanceTrends(clientId, profile.id)` from analytics queries
       - Return `{ success: true, data: FamilyAnalyticsData }` or `{ success: false, error: string }`
       - Import getFamilyGovernanceTrends from `@/lib/analytics/queries`

    2. Create `src/app/(protected)/advisor/analytics/[clientId]/page.tsx`:
       - Server component with Suspense streaming (same pattern as advisor dashboard page)
       - Hero section with "Family Analytics" kicker, family name as title, description about governance trends
       - Back link to `/advisor/dashboard` using Lucide ArrowLeft icon
       - Suspense-wrapped async AnalyticsContent component that calls getFamilyAnalyticsData
       - Layout sections (vertical stack, space-y-6):
         a. Latest assessment summary: overall score (large text), trend indicator, assessment count, latest date
         b. GovernanceTrendChart with the trendData
         c. CategoryBreakdownChart with the latestBreakdown
         d. AssessmentComparisonView with assessments array
       - Error handling: show error message if server action fails
       - Empty state: if no completed assessments, show "No completed assessments yet" message with guidance

    3. Create `src/app/(protected)/advisor/analytics/[clientId]/loading.tsx`:
       - Skeleton matching the analytics page structure: hero placeholder, chart skeletons (rounded rectangles with pulse animation), category chart skeleton
       - Follow same skeleton pattern as existing dashboard loading.tsx

    4. Modify `src/components/dashboard/GovernanceTable.tsx`:
       - Make family name cells clickable links to `/advisor/analytics/{clientId}`
       - Import Next.js `Link` component
       - Wrap the name text in each row with `<Link href={/advisor/analytics/${row.original.id}} className="hover:underline text-primary">`. Only link families that have at least 1 completed assessment (assessmentCount > 0). Non-assessed families show plain text.
  </action>
  <verify>
    Run `npm run build` — build succeeds with no errors. Verify the analytics page route exists at `src/app/(protected)/advisor/analytics/[clientId]/page.tsx`. Verify GovernanceTable.tsx contains Link import and href to analytics route. Verify loading.tsx exists.
  </verify>
  <done>Family analytics page renders at /advisor/analytics/[clientId] with governance trend line chart, category breakdown bar chart, trend indicators, and assessment comparison. Dashboard table family names link to analytics page for assessed families. Loading skeleton provides streaming fallback.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds with zero errors
- Route `/advisor/analytics/[clientId]` exists with page.tsx and loading.tsx
- GovernanceTrendChart renders LineChart with Y-axis domain [0, 10]
- CategoryBreakdownChart renders BarChart with all 8 governance categories
- TrendIndicator shows improving (green up), declining (red down), stable, new
- GovernanceTable family names are clickable links to analytics page
- Server action enforces advisor auth via requireAdvisorRole
- Suspense streaming used for data-dependent content
- VIZ-01: Historical score trend line chart present
- VIZ-02: Trend indicators (improving/declining) shown between assessments
- VIZ-03: Category breakdown chart with 8 governance domains
- VIZ-04: Assessment comparison view with side-by-side periods
- VIZ-05: Charts handle 8 categories with 0-10 score scale
</verification>

<success_criteria>
Advisor can click a family name in the governance dashboard, navigate to the family analytics page, and see: a line chart showing governance score history over time, a bar chart breaking down all 8 governance categories, trend indicators showing improving/declining/stable between assessments, and a side-by-side comparison of the two most recent assessment periods. All charts render correctly with governance scoring data on a 0-10 scale.
</success_criteria>

<output>
After completion, create `.planning/phases/12-analytics-engine/12-02-SUMMARY.md`
</output>
