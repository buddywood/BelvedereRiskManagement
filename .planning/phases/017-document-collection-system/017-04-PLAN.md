---
phase: 017-document-collection-system
plan: 04
type: execute
wave: 1
depends_on: []
files_modified: [scripts/seed-advisor-test-data.js]
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "After running seed script, client@test.com sees document requirements on /documents page instead of empty state"
    - "Document portal displays advisor firm name prominently with 'Document Collection Portal' subtitle"
    - "Requirements show correct status badges (Required, Optional) and progress indicator"
  artifacts:
    - path: "scripts/seed-advisor-test-data.js"
      provides: "DocumentRequirement seed records for both test clients"
      contains: "documentRequirement"
  key_links:
    - from: "scripts/seed-advisor-test-data.js"
      to: "prisma.documentRequirement"
      via: "Prisma upsert/create calls"
      pattern: "prisma\\.documentRequirement\\."
---

<objective>
Add DocumentRequirement seed records to the test data script so the client document portal displays requirements instead of empty state.

Purpose: UAT test 1 failed because the seed script creates advisor profiles and client assignments but no document requirements. The documents page correctly shows empty state when no requirements exist -- it needs data to render the branded portal with requirements list.

Output: Updated seed script that creates realistic document requirements for test clients.
</objective>

<execution_context>
@/Users/bwoodtalton/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bwoodtalton/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/017-document-collection-system/017-UAT.md
@scripts/seed-advisor-test-data.js
@prisma/schema.prisma (DocumentRequirement model)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add DocumentRequirement records to seed script</name>
  <files>scripts/seed-advisor-test-data.js</files>
  <action>
After the client-advisor assignment creation for the first client (client@test.com), add DocumentRequirement records using prisma.documentRequirement.upsert(). Create 4 requirements for the first test client:

1. "Trust Agreement" - required: true, fulfilled: false, description: "Current family trust documentation"
2. "Tax Return (Most Recent)" - required: true, fulfilled: false, description: "Most recent federal tax return"
3. "Estate Plan Summary" - required: true, fulfilled: false, description: "Summary of current estate planning documents"
4. "Insurance Policies" - required: false (optional), fulfilled: false, description: "Life insurance and liability coverage details"

Each requirement must reference:
- advisorId: advisorProfile.id (the AdvisorProfile record id, NOT the user id)
- clientId: clientUser.id (the User record id)

Use upsert with a deterministic id pattern (e.g., `doc-req-${clientUser.id}-1`) to make the script idempotent. The DocumentRequirement model uses cuid() for id by default, but providing explicit ids enables upsert.

Also add 2 requirements for the second client (client-mfa@test.com):
1. "Trust Agreement" - required: true, fulfilled: false
2. "Financial Statements" - required: true, fulfilled: false

Log the count of created requirements.

Do NOT modify any other part of the seed script. Insert the new code in the appropriate location after each client's assignment block.
  </action>
  <verify>
Run: node scripts/seed-advisor-test-data.js

Verify output includes log line confirming document requirements were created. No errors should appear. Script should be idempotent (running twice produces same result).
  </verify>
  <done>Seed script creates 4 DocumentRequirement records for client@test.com and 2 for client-mfa@test.com. Running seed then navigating to /documents as client@test.com shows the branded document portal with requirements list instead of empty state.</done>
</task>

</tasks>

<verification>
1. Run seed script: `node scripts/seed-advisor-test-data.js` -- completes without errors
2. Run seed script again -- idempotent, no duplicate errors
3. Log in as client@test.com, navigate to /documents -- sees document requirements with advisor branding, NOT empty state
4. Progress indicator shows "0 of 3 documents uploaded" (3 required out of 4 total)
</verification>

<success_criteria>
- Seed script creates DocumentRequirement records linking advisor profile to test clients
- Document portal shows requirements list with status badges when logged in as client@test.com
- UAT test 1 passes: portal displays advisor firm name and document requirements
</success_criteria>

<output>
After completion, create `.planning/phases/017-document-collection-system/017-04-SUMMARY.md`
</output>
