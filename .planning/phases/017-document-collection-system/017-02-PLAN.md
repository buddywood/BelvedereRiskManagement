---
phase: 017-document-collection-system
plan: 02
type: execute
wave: 2
depends_on: ["017-01"]
files_modified:
  - src/app/(protected)/documents/page.tsx
  - src/app/(protected)/documents/layout.tsx
  - src/components/documents/DocumentUpload.tsx
  - src/components/documents/DocumentList.tsx
  - src/components/documents/ClientDocumentPortal.tsx
autonomous: true

must_haves:
  truths:
    - "Client can see all required documents with their status (uploaded vs missing)"
    - "Client can drag-and-drop or click to upload files for each requirement"
    - "Client portal displays advisor branding (firm name, logo) throughout"
    - "Upload progress and success/error feedback is shown to the client"
    - "Uploaded documents show file name, size, and upload date"
  artifacts:
    - path: "src/app/(protected)/documents/page.tsx"
      provides: "Client document portal page"
      min_lines: 20
    - path: "src/app/(protected)/documents/layout.tsx"
      provides: "Document portal layout with branding"
      min_lines: 15
    - path: "src/components/documents/DocumentUpload.tsx"
      provides: "Drag-drop upload component using react-dropzone"
      min_lines: 50
    - path: "src/components/documents/DocumentList.tsx"
      provides: "List of required documents with status indicators"
      min_lines: 40
    - path: "src/components/documents/ClientDocumentPortal.tsx"
      provides: "Main portal component orchestrating upload and list"
      min_lines: 40
  key_links:
    - from: "src/components/documents/DocumentUpload.tsx"
      to: "/api/documents/upload-url"
      via: "fetch to get presigned URL then PUT to S3"
      pattern: "fetch.*upload-url"
    - from: "src/components/documents/DocumentUpload.tsx"
      to: "src/lib/actions/document-actions.ts"
      via: "confirmDocumentUpload after S3 upload"
      pattern: "confirmDocumentUpload"
    - from: "src/app/(protected)/documents/page.tsx"
      to: "src/lib/actions/document-actions.ts"
      via: "getClientDocumentRequirements for initial data"
      pattern: "getClientDocumentRequirements"
---

<objective>
Build the client-facing document portal with branded interface, drag-drop file uploads, and document requirement tracking.

Purpose: Enable clients to upload required documents through a professional, advisor-branded portal that tracks completion status (DOC-03, DOC-04, BRAND-03).
Output: Client document portal page with upload functionality, status tracking, and advisor branding.
</objective>

<execution_context>
@/Users/bwoodtalton/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bwoodtalton/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/017-document-collection-system/017-RESEARCH.md
@.planning/phases/017-document-collection-system/017-01-SUMMARY.md
@src/lib/documents/types.ts
@src/lib/documents/s3.ts
@src/lib/actions/document-actions.ts
@src/lib/invitations/email.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DocumentUpload and DocumentList components</name>
  <files>
    src/components/documents/DocumentUpload.tsx
    src/components/documents/DocumentList.tsx
  </files>
  <action>
1. Create src/components/documents/DocumentUpload.tsx:
   - "use client" component using react-dropzone (useDropzone hook)
   - Props: { requirementId: string, requirementName: string, onUploadComplete: () => void }
   - Accept file types from ALLOWED_FILE_TYPES constant, maxSize from MAX_FILE_SIZE
   - Upload flow:
     a. User drops/selects file
     b. Show upload progress state ("Requesting upload URL...", "Uploading...", "Confirming...")
     c. POST to /api/documents/upload-url with { requirementId, fileName, fileType, fileSize }
     d. PUT file directly to S3 using returned signedUrl
     e. Call confirmDocumentUpload server action with file metadata
     f. Show success message, call onUploadComplete
   - Handle errors at each step with user-friendly messages
   - Styling: Tailwind classes matching existing app patterns. Dashed border drop zone, hover/active states, file type hint text ("PDF, PNG, JPG up to 10MB")
   - Drag active state: blue border/background highlight

2. Create src/components/documents/DocumentList.tsx:
   - "use client" component
   - Props: { requirements: DocumentRequirement[], advisorFirmName: string, onUploadComplete: () => void }
   - Display each requirement as a card:
     - Requirement name and description
     - Status badge: "Uploaded" (green) or "Required" (amber) or "Optional" (gray)
     - If fulfilled: show fileName, fileSize (formatted human-readable), fulfilledAt date
     - If not fulfilled: render DocumentUpload component inline for that requirement
   - Progress summary at top: "X of Y documents uploaded" with progress bar
   - Sort: unfulfilled requirements first, then fulfilled
  </action>
  <verify>
    npx tsc --noEmit
  </verify>
  <done>
    DocumentUpload handles full presigned URL upload flow with progress states. DocumentList displays all requirements with status indicators and inline upload for missing documents. Components compile without errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create client document portal page with advisor branding</name>
  <files>
    src/components/documents/ClientDocumentPortal.tsx
    src/app/(protected)/documents/page.tsx
    src/app/(protected)/documents/layout.tsx
  </files>
  <action>
1. Create src/components/documents/ClientDocumentPortal.tsx:
   - "use client" component
   - Props: { requirements: array of document requirements, advisorInfo: { firmName, logoUrl } }
   - Renders advisor branding header:
     - If logoUrl exists and is HTTPS: display logo image (max-height 48px)
     - Display firmName prominently
     - Subtitle: "Document Collection Portal"
   - Renders DocumentList with requirements
   - Handles refresh after upload by re-fetching via getClientDocumentRequirements
   - Uses useRouter for refresh or useState to manage requirement list locally
   - Empty state: "No document requirements yet. Your advisor will request documents when needed."

2. Create src/app/(protected)/documents/layout.tsx:
   - Simple layout wrapper with consistent padding/max-width
   - Page title metadata: "Documents"

3. Create src/app/(protected)/documents/page.tsx:
   - Server component
   - Call getClientDocumentRequirements() server action
   - Handle error state (show message if action fails)
   - Pass data to ClientDocumentPortal component
   - If no requirements exist, show the empty state via the portal component

4. Add "Documents" link to the client navigation. Check src/components/layout/ProtectedNav.tsx for existing nav patterns. Add a "Documents" link that appears for non-advisor users (clients). Use a document/folder icon consistent with existing nav icons. Link to /documents.
  </action>
  <verify>
    npx tsc --noEmit
  </verify>
  <done>
    Client document portal at /documents shows advisor branding, lists all document requirements with upload capability, tracks completion progress. Navigation link added for client users. TypeScript compiles cleanly.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- All component files exist at specified paths
- DocumentUpload uses react-dropzone (not custom drag-drop)
- Upload flow: presigned URL -> S3 PUT -> confirm (not server upload)
- Advisor branding (firmName, logoUrl) displayed in portal header
- Document status tracking shows uploaded vs missing counts
- Client navigation includes Documents link
</verification>

<success_criteria>
Clients can navigate to /documents, see their advisor's branding, view all required documents with status, and upload files via drag-drop. Uploaded files go directly to S3 via presigned URLs. Upload completion updates the requirement status in real-time.
</success_criteria>

<output>
After completion, create `.planning/phases/017-document-collection-system/017-02-SUMMARY.md`
</output>
