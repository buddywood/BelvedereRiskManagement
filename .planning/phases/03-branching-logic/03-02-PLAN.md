---
phase: 03-branching-logic
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/lib/hooks/useAssessmentNavigation.ts
  - src/lib/assessment/store.ts
  - src/app/(protected)/assessment/[pillarSlug]/[questionIndex]/page.tsx
  - src/app/(protected)/assessment/page.tsx
  - src/app/(protected)/assessment/complete/page.tsx
autonomous: true
user_setup: []

must_haves:
  truths:
    - "When user changes a gate answer, newly-relevant questions appear seamlessly without user awareness of skipping"
    - "When user changes a gate answer, they are auto-navigated to the first newly-relevant question"
    - "Scores recalculate in real-time as answers change or sections become relevant/irrelevant"
    - "Review mode only shows applicable questions (hidden questions never shown)"
    - "User completes assessment in 12-15 minutes with branching active"
    - "Progress percentage reflects only visible questions (hidden questions excluded from denominator)"
  artifacts:
    - path: "src/lib/hooks/useAssessmentNavigation.ts"
      provides: "Branching change detection and auto-navigation on gate answer change"
    - path: "src/lib/assessment/store.ts"
      provides: "Real-time score recalculation action, orphan cleanup on answer change"
    - path: "src/app/(protected)/assessment/[pillarSlug]/[questionIndex]/page.tsx"
      provides: "Question page with branching change handling"
    - path: "src/app/(protected)/assessment/page.tsx"
      provides: "Assessment hub with branching-aware progress and review"
    - path: "src/app/(protected)/assessment/complete/page.tsx"
      provides: "Completion page using visible question count"
  key_links:
    - from: "src/app/(protected)/assessment/[pillarSlug]/[questionIndex]/page.tsx"
      to: "src/lib/assessment/branching.ts"
      via: "detectBranchingChanges called after answer save"
      pattern: "detectBranchingChanges"
    - from: "src/lib/hooks/useAssessmentNavigation.ts"
      to: "src/lib/assessment/branching.ts"
      via: "uses getVisibleQuestions for navigation"
      pattern: "getVisibleQuestions"
    - from: "src/lib/assessment/store.ts"
      to: "src/lib/assessment/branching.ts"
      via: "getOrphanedAnswerIds for cleanup"
      pattern: "getOrphanedAnswerIds"
---

<objective>
Wire branching change detection into the assessment UI for seamless adaptive question flow.

Purpose: Phase 3 user decisions require that when a gate answer changes, the system auto-navigates to newly-relevant questions, recalculates scores in real-time, and hides skipping completely from the user. This plan integrates the engine functions from Plan 01 into the React components and Zustand store.

Output: Assessment UI that seamlessly adapts to branching changes with auto-navigation, real-time scoring, and clean review experience.
</objective>

<execution_context>
@/Users/bwoodtalton/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bwoodtalton/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-branching-logic/03-01-SUMMARY.md
@src/lib/hooks/useAssessmentNavigation.ts
@src/lib/assessment/store.ts
@src/app/(protected)/assessment/[pillarSlug]/[questionIndex]/page.tsx
@src/app/(protected)/assessment/page.tsx
@src/app/(protected)/assessment/complete/page.tsx
@src/lib/assessment/branching.ts
@src/lib/assessment/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Store and navigation enhancements for branching re-evaluation</name>
  <files>src/lib/assessment/store.ts, src/lib/hooks/useAssessmentNavigation.ts</files>
  <action>
1. **Update store.ts** - Add a `cleanOrphanedAnswers` action that:
   - Imports `getOrphanedAnswerIds` from branching.ts and `allQuestions` from questions.ts
   - Calls `getOrphanedAnswerIds(state.answers, allQuestions)` to find orphaned answer IDs
   - Does NOT delete orphaned answers from the store (they may become relevant again if gate changes back). Instead, stores them in a new `orphanedAnswerIds: string[]` state field
   - Updates `orphanedAnswerIds` whenever `setAnswer` is called (call cleanOrphanedAnswers internally after each answer set)
   - Add `orphanedAnswerIds` to the persisted state (partialize)

2. **Update useAssessmentNavigation.ts** - Add branching change detection:
   - Import `detectBranchingChanges` from branching.ts
   - Add a `usePrevious` ref pattern to track previous answers state
   - When the visible question set changes (detected via comparing previous vs current visibleQuestions), compute the diff using `detectBranchingChanges`
   - Export a new `branchingChange` field: `{ newlyVisible: string[], newlyHidden: string[] } | null`
   - This field is consumed by the question page component to trigger auto-navigation
   - Ensure progress calculation continues to use only visible questions (already correct, just verify)
   - When the current question becomes hidden (due to branching change), automatically adjust currentIndex to the nearest visible question

Important: Per user decision "Hide skipping completely - seamless experience where user never knows questions were skipped" - no UI indicators of skipping should be added. The branching just makes certain questions not appear.
  </action>
  <verify>
Run `npx tsc --noEmit` - no type errors.
Run `npm run build` - build succeeds.
  </verify>
  <done>
Store tracks orphaned answer IDs and updates on every answer change. Navigation hook detects branching changes and exports diff for UI consumption. Current question auto-adjusts when hidden by branching.
  </done>
</task>

<task type="auto">
  <name>Task 2: Question page branching integration and review mode</name>
  <files>src/app/(protected)/assessment/[pillarSlug]/[questionIndex]/page.tsx, src/app/(protected)/assessment/page.tsx, src/app/(protected)/assessment/complete/page.tsx</files>
  <action>
1. **Update question page** (`[pillarSlug]/[questionIndex]/page.tsx`):
   - After `handleAnswer` saves an answer, check if the answered question is a gate question (teg-01, bi-01, sp-01) by checking if `branchingChange` from useAssessmentNavigation has newlyVisible items
   - Per user decision: "Auto-navigate to first newly-relevant question immediately when branching changes occur" - when `branchingChange.newlyVisible` is non-empty, use `useEffect` to navigate to the first newly-visible question's index in the visibleQuestions array
   - When `branchingChange.newlyHidden` is non-empty and current question is hidden, navigate to the nearest valid question (handled by navigation hook from Task 1)
   - Do NOT show any toast/message about questions being shown/hidden (per "hide skipping completely" decision)
   - Ensure the progress bar (`SectionProgress`) continues to show percentage based on visible questions only

2. **Update assessment hub** (`page.tsx`):
   - Per user decision: "Only show applicable questions in review mode (cleaner, focused experience)" - the "Review Answers from Beginning" button already navigates to question 0 which uses visible questions. Verify this works correctly: when reviewing, the user only sees questions visible based on current answers. No changes needed if already correct, but verify the flow.
   - Update `handleContinueAssessment` to also call `store.cleanOrphanedAnswers()` before computing smart resume position, ensuring orphaned answers don't affect resume calculation
   - Ensure the question count in the Welcome Back banner uses visible questions (already does via `getVisibleQuestions` - verify)

3. **Update completion page** (`complete/page.tsx`):
   - Before triggering score calculation, the page should reflect branching-aware question count
   - No functional changes likely needed since scoring API (updated in Plan 01) handles branching, but verify the completion check uses visible questions

Per user decision: "Don't mention time savings or personalization - just deliver fast 12-minute experience" - no UI copy should reference branching, skipping, or time savings. The experience should feel like a normal, focused questionnaire.
  </action>
  <verify>
Run `npx tsc --noEmit` - no type errors.
Run `npm run build` - build succeeds.
Verify no UI text mentions "skipping", "branching", "personalized", or "time saved".
  </verify>
  <done>
Gate answer changes trigger auto-navigation to newly-relevant questions. Review mode shows only applicable questions. No UI indication of branching/skipping exists. Progress reflects visible questions only. Build clean.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` - clean build with no errors
2. `npx tsc --noEmit` - no type errors
3. No UI text contains "skip", "branch", "personalize" - grep verification
4. Changing a gate answer in the question flow re-evaluates visibility
5. Review from beginning only shows applicable questions
</verification>

<success_criteria>
- Gate answer changes trigger re-evaluation of visible questions
- Auto-navigation to first newly-relevant question on branching change
- Orphaned answers tracked in store but preserved (not deleted)
- Scoring excludes orphaned answers via visible question filtering
- Review mode shows only applicable questions
- No UI text references skipping or branching
- Progress percentage uses visible question denominator
- Build and TypeScript compilation clean
</success_criteria>

<output>
After completion, create `.planning/phases/03-branching-logic/03-02-SUMMARY.md`
</output>
