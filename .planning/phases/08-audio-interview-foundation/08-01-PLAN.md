---
phase: 08-audio-interview-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - src/lib/intake/types.ts
  - src/lib/intake/questions.ts
  - src/lib/schemas/intake.ts
autonomous: true

must_haves:
  truths:
    - "IntakeInterview and IntakeResponse models exist in database"
    - "Interview questions are defined with text, context, and recording tips"
    - "TypeScript types and Zod schemas validate intake data"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "IntakeInterview and IntakeResponse models"
      contains: "model IntakeInterview"
    - path: "src/lib/intake/types.ts"
      provides: "TypeScript types for intake system"
      exports: ["IntakeQuestion", "IntakeStatus"]
    - path: "src/lib/intake/questions.ts"
      provides: "Interview question definitions"
      exports: ["INTAKE_QUESTIONS", "TOTAL_QUESTIONS"]
    - path: "src/lib/schemas/intake.ts"
      provides: "Zod validation schemas for intake"
      exports: ["saveResponseSchema"]
  key_links:
    - from: "src/lib/intake/types.ts"
      to: "prisma/schema.prisma"
      via: "mirrors Prisma enum types"
      pattern: "IntakeStatus|TranscriptionStatus"
---

<objective>
Create the data foundation for intake interviews: Prisma models, TypeScript types, interview question bank, and Zod validation schemas.

Purpose: Establishes the database schema and type system that all other intake plans build on.
Output: Prisma models migrated, question bank defined, types and schemas compile cleanly.
</objective>

<execution_context>
@/Users/bwoodtalton/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bwoodtalton/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@prisma/schema.prisma
@src/lib/schemas/profile.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prisma models and enums for intake interviews</name>
  <files>prisma/schema.prisma</files>
  <action>
    Add Prisma models to schema.prisma:

    **IntakeStatus enum:** NOT_STARTED, IN_PROGRESS, COMPLETED, SUBMITTED
    **TranscriptionStatus enum:** PENDING, PROCESSING, COMPLETED, FAILED

    **IntakeInterview model:**
    - id (cuid), userId (relation to User), status (IntakeStatus enum: default NOT_STARTED), currentQuestionIndex (Int, default 0), startedAt (DateTime?), completedAt (DateTime?), submittedAt (DateTime?), updatedAt (DateTime @updatedAt)
    - Add `intakeInterviews IntakeInterview[]` relation on User model

    **IntakeResponse model:**
    - id (cuid), interviewId (relation to IntakeInterview), questionId (String), audioUrl (String?), audioDuration (Float?), transcription (String?), transcriptionStatus (TranscriptionStatus enum: default PENDING), answeredAt (DateTime?), updatedAt (DateTime @updatedAt)
    - Unique constraint on [interviewId, questionId]
    - Add `responses IntakeResponse[]` relation on IntakeInterview model

    Run `npx prisma db push` to apply.
  </action>
  <verify>
    Run `npx prisma db push` succeeds without errors.
    Run `npx prisma generate` succeeds.
  </verify>
  <done>
    IntakeInterview and IntakeResponse models exist in database with proper relations and enums.
  </done>
</task>

<task type="auto">
  <name>Task 2: TypeScript types, question bank, and Zod schemas</name>
  <files>
    src/lib/intake/types.ts
    src/lib/intake/questions.ts
    src/lib/schemas/intake.ts
  </files>
  <action>
    1. Create src/lib/intake/types.ts:
    - IntakeQuestion type: { id: string, questionNumber: number, questionText: string, context: string, recordingTips: string[] }
    - IntakeStatus type matching Prisma enum
    - IntakeInterviewWithResponses type (Prisma include pattern with responses relation)

    2. Create src/lib/intake/questions.ts:
    - Export INTAKE_QUESTIONS array with 8-12 questions covering family governance topics:
      - Family structure and dynamics
      - Current governance practices
      - Decision-making processes
      - Wealth transfer concerns
      - Communication patterns
      - Conflict resolution approaches
      - Succession planning status
      - Risk awareness and priorities
    - Each question includes: id (e.g. "intake-q1"), questionNumber, questionText (clear, conversational tone), context (1-2 sentences explaining why this matters), recordingTips (2-3 tips for a good audio response)
    - Export TOTAL_QUESTIONS constant

    3. Create src/lib/schemas/intake.ts following pattern in src/lib/schemas/profile.ts:
    - Zod schemas: startInterviewSchema, saveResponseSchema (questionId, audioUrl?, transcription?), submitInterviewSchema
  </action>
  <verify>
    Run `npx tsc --noEmit` to verify types compile.
    Verify INTAKE_QUESTIONS has 8-12 entries with all required fields.
  </verify>
  <done>
    TypeScript types, question definitions with 8-12 questions, and Zod schemas are defined and compile cleanly.
  </done>
</task>

</tasks>

<verification>
- `npx prisma db push` applies schema changes successfully
- `npx tsc --noEmit` passes with zero errors
- IntakeInterview and IntakeResponse tables exist in database
- INTAKE_QUESTIONS array has 8-12 well-formed questions
</verification>

<success_criteria>
Database models for intake interviews exist. Question bank with 8-12 questions is defined with text, context, and recording tips. Types and Zod schemas compile cleanly.
</success_criteria>

<output>
After completion, create `.planning/phases/08-audio-interview-foundation/08-01-SUMMARY.md`
</output>
