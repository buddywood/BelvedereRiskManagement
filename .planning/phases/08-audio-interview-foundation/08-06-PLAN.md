---
phase: 08-audio-interview-foundation
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(protected)/intake/interview/page.tsx
  - src/app/api/intake/[id]/transcribe/route.ts
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "User can navigate step-by-step intake interview without loading failures"
    - "Step indicator accurately reflects which questions have been completed"
    - "User can record and save audio without being blocked by transcription failures"
    - "User receives confirmation when interview is completed and submitted"
  artifacts:
    - path: "src/app/(protected)/intake/interview/page.tsx"
      provides: "Working interview wizard with correct imports, step indicator, and error handling"
      contains: "getActiveIntakeInterviewAction"
    - path: "src/app/api/intake/[id]/transcribe/route.ts"
      provides: "Transcription endpoint with graceful degradation"
  key_links:
    - from: "src/app/(protected)/intake/interview/page.tsx"
      to: "src/lib/actions/intake-actions.ts"
      via: "top-level import of getActiveIntakeInterviewAction"
      pattern: "import.*getActiveIntakeInterviewAction.*from"
    - from: "src/app/(protected)/intake/interview/page.tsx"
      to: "src/lib/intake/questions.ts"
      via: "import INTAKE_QUESTIONS for step indicator mapping"
      pattern: "import.*INTAKE_QUESTIONS.*from"
---

<objective>
Fix 4 critical bugs preventing the intake interview from functioning: dynamic import hydration issue, broken step indicator logic, transcription error handling, and submission flow.

Purpose: Phase 08 is completely non-functional due to these bugs. All 4 verification truths fail.
Output: Working interview wizard that users can navigate, record responses, and complete.
</objective>

<execution_context>
@/Users/bwoodtalton/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bwoodtalton/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-audio-interview-foundation/08-VERIFICATION-CORRECTED.md
@src/lib/intake/questions.ts
@src/lib/hooks/useIntakeInterview.ts
@src/lib/actions/intake-actions.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix interview page - dynamic import, step indicator, and error resilience</name>
  <files>src/app/(protected)/intake/interview/page.tsx</files>
  <action>
Fix 3 bugs in the interview page:

**Bug 1: Dynamic import (line 55-56)**
Replace the dynamic `import("@/lib/actions/intake-actions").then(actions => actions.getActiveIntakeInterviewAction())` with a direct function call. Add `getActiveIntakeInterviewAction` to the existing top-level import from `@/lib/actions/intake-actions` (line 16-19). Then call it directly: `const activeInterviewResult = await getActiveIntakeInterviewAction();`

**Bug 2: Broken step indicator (lines 262-274)**
Import `INTAKE_QUESTIONS` from `@/lib/intake/questions` at the top of the file. Replace the broken `.find()` callback that always returns `true` with a proper lookup that maps questionId to its index in INTAKE_QUESTIONS:

```
completedSteps={new Set(
  Object.entries(responses)
    .filter(([, response]) => response?.status === 'completed')
    .map(([questionId]) => {
      const questionIndex = INTAKE_QUESTIONS.findIndex(q => q.id === questionId);
      return questionIndex >= 0 ? questionIndex : -1;
    })
    .filter(index => index >= 0)
)}
```

**Bug 3: Error resilience in handleRecordingComplete**
The existing error handling for transcription failures is mostly correct (lines 130-174 already catch failures and mark as completed). However, add a guard to prevent the auto-submission block (lines 177-194) from running if the upload itself failed. Currently if upload fails (line 117), it throws and skips submission correctly. Verify this path is solid.

Also add a `disabled` state check at the top of `handleRecordingComplete` to prevent double-invocation: if `uploading` or `submitting` is true, return early.
  </action>
  <verify>
Run `npx tsc --noEmit` to verify no TypeScript errors. Grep the file to confirm:
1. No dynamic `import(` calls remain
2. `getActiveIntakeInterviewAction` appears in top-level imports
3. `INTAKE_QUESTIONS` is imported and used in step indicator
4. The `.find(i => { return true; })` pattern is gone
  </verify>
  <done>
Interview page loads without hydration issues (no dynamic import), step indicator correctly maps completed question IDs to step indices using INTAKE_QUESTIONS, and double-invocation is prevented.
  </done>
</task>

<task type="auto">
  <name>Task 2: Harden transcription endpoint and ensure graceful degradation</name>
  <files>src/app/api/intake/[id]/transcribe/route.ts</files>
  <action>
The transcription endpoint already handles missing OPENAI_API_KEY by returning a placeholder (lines 68-83), which is good. Make these improvements:

1. **Return 200 (not 500) when API key is missing.** The current code already does this correctly (line 79 returns NextResponse.json without error status). Verify this path works by checking it returns `{ success: true, transcription: "..." }`.

2. **Add request-level timeout.** Wrap the Whisper API fetch call (line 91) with an AbortController that times out after 30 seconds. If timeout occurs, save response with FAILED status and return a 200 with `{ success: true, transcription: "[Transcription timed out - will be retried later]" }` so the client is never blocked.

3. **Improve error logging.** Add structured log messages that include interviewId and questionId for debugging. Replace bare `console.error` calls with messages that include context: `console.error('[Transcribe]', { interviewId, questionId, error: ... })`.

4. **Handle file read errors gracefully.** The catch block at line 131 currently returns 400. Change it to return 200 with a placeholder transcription similar to the missing API key case, so the client flow is never blocked by transcription infrastructure issues. Save with status FAILED so it can be retried later.

The key principle: transcription is a BEST-EFFORT operation. The client should NEVER be blocked by transcription failures. Always return success with a placeholder if the actual transcription fails.
  </action>
  <verify>
Run `npx tsc --noEmit` to verify no TypeScript errors. Read the file and confirm:
1. Missing API key path returns 200 with success: true
2. AbortController wraps the Whisper fetch
3. File read errors return 200 with placeholder transcription
4. All error paths save FAILED status to DB for later retry
  </verify>
  <done>
Transcription endpoint never returns errors that would block the client interview flow. All failure modes (missing API key, timeout, file read error, Whisper API error) return 200 with placeholder text and save FAILED status for retry.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `npx tsc --noEmit` passes with zero errors
2. `npm run build` succeeds (or `next build`)
3. Interview page has no dynamic imports in useEffect
4. Step indicator uses INTAKE_QUESTIONS.findIndex for proper mapping
5. Transcription endpoint returns 200 on all failure modes
</verification>

<success_criteria>
All 4 critical bugs from VERIFICATION-CORRECTED.md are resolved:
- Dynamic import replaced with static import (bug 1)
- Step indicator shows accurate progress (bug 2)
- No infinite loops from transcription failures (bug 3)
- Auto-submission not blocked by transcription errors (bug 4)
</success_criteria>

<output>
After completion, create `.planning/phases/08-audio-interview-foundation/08-06-SUMMARY.md`
</output>
