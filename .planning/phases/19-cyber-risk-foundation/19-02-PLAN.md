---
phase: 19-cyber-risk-foundation
plan: 02
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - src/app/(protected)/assessment/page.tsx
  - src/app/(protected)/assessment/[pillarSlug]/[questionIndex]/page.tsx
  - src/app/api/assessment/[id]/score/route.ts
  - src/lib/assessment/store.ts
  - src/app/(protected)/assessment/results/page.tsx
autonomous: true

must_haves:
  truths:
    - "Family can start and complete a cyber risk assessment separate from governance"
    - "Assessment hub shows both governance and cyber risk pillars with independent progress"
    - "Cyber risk scores are calculated on 0-10 scale matching governance"
    - "Assessment results page displays cyber risk scores when cyber pillar completed"
  artifacts:
    - path: "src/app/(protected)/assessment/page.tsx"
      provides: "Multi-pillar assessment hub with governance and cyber risk cards"
      contains: "cyber-risk"
    - path: "src/app/api/assessment/[id]/score/route.ts"
      provides: "Score calculation supporting both governance and cyber-risk pillars"
      contains: "cyber-risk"
    - path: "src/lib/assessment/store.ts"
      provides: "Assessment store supporting multi-pillar tracking"
      contains: "cyber-risk"
  key_links:
    - from: "src/app/(protected)/assessment/page.tsx"
      to: "src/lib/cyber-risk/questions.ts"
      via: "imports cyberRiskPillar for pillar card"
      pattern: "import.*cyberRiskPillar"
    - from: "src/app/api/assessment/[id]/score/route.ts"
      to: "src/lib/cyber-risk/scoring.ts"
      via: "calls calculateCyberRiskScore for cyber pillar"
      pattern: "calculateCyberRiskScore"
    - from: "src/app/(protected)/assessment/[pillarSlug]/[questionIndex]/page.tsx"
      to: "src/lib/cyber-risk/questions.ts"
      via: "loads cyber questions when pillarSlug is cyber-risk"
      pattern: "cyber-risk.*cyberRiskQuestions"
---

<objective>
Enable multi-pillar assessment flow: extend the assessment hub, question flow, scoring API, and results to support cyber risk alongside governance.

Purpose: Completes CYBER-01 (core scoring engine as user-facing feature) by wiring the cyber risk question bank into the existing assessment infrastructure. Families can start, complete, and receive scored results for cyber risk.

Output: Working multi-pillar assessment with cyber risk as second pillar.
</objective>

<execution_context>
@/Users/bwoodtalton/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bwoodtalton/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-cyber-risk-foundation/19-01-SUMMARY.md
@src/app/(protected)/assessment/page.tsx
@src/app/(protected)/assessment/[pillarSlug]/[questionIndex]/page.tsx
@src/app/api/assessment/[id]/score/route.ts
@src/lib/assessment/store.ts
@src/app/(protected)/assessment/results/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend assessment hub for multi-pillar support</name>
  <files>src/app/(protected)/assessment/page.tsx, src/lib/assessment/store.ts</files>
  <action>
**Update `src/app/(protected)/assessment/page.tsx`:**
- Import `cyberRiskPillar` and `allCyberQuestions` from `@/lib/cyber-risk/questions`
- Change from single FAMILY_GOVERNANCE_PILLAR constant to an array of pillar configs: `[{ pillar: familyGovernancePillar, questions: allQuestions }, { pillar: cyberRiskPillar, questions: allCyberQuestions }]`
- Render a PillarCard for each pillar instead of just one
- Each PillarCard tracks its own status (not-started/in-progress/completed) based on `store.completedPillars`
- Each PillarCard shows independent question count and answered count
- handleStartAssessment and handleContinueAssessment must accept a pillar slug parameter
- Update OverallProgress to show `totalPillars: 2`
- Layout: Stack pillar cards vertically in the "Assessment Section" grid
- "Next Step" section should show context-appropriate text based on overall assessment state (both pillars)
- Keep existing customization logic for governance pillar only (cyber risk does not have advisor customization in this phase)

**Update `src/lib/assessment/store.ts`:**
- The store already tracks `currentPillar` and `completedPillars` as strings, which already supports multi-pillar. Verify this works with 'cyber-risk' as a pillar value.
- If `setCurrentPosition` or other methods hardcode 'family-governance', generalize them.
- No changes needed if store is already pillar-agnostic (it likely is based on the interface).
  </action>
  <verify>
Run `npx tsc --noEmit` on the modified files -- no type errors.
Start dev server, navigate to /assessment -- both pillar cards visible.
Click "Begin" on cyber risk card -- navigates to /assessment/cyber-risk/0.
  </verify>
  <done>
Assessment hub displays governance and cyber risk as separate pillar cards.
Each pillar tracks independent progress.
Users can start either assessment independently.
  </done>
</task>

<task type="auto">
  <name>Task 2: Enable cyber risk question flow and scoring</name>
  <files>src/app/(protected)/assessment/[pillarSlug]/[questionIndex]/page.tsx, src/app/api/assessment/[id]/score/route.ts</files>
  <action>
**Update `src/app/(protected)/assessment/[pillarSlug]/[questionIndex]/page.tsx`:**
- Currently this page likely loads questions from `allQuestions` (governance only)
- Add logic to resolve questions based on `pillarSlug` param:
  - If pillarSlug is 'family-governance': use existing `allQuestions` from `@/lib/assessment/questions`
  - If pillarSlug is 'cyber-risk': use `allCyberQuestions` from `@/lib/cyber-risk/questions`
- Create a helper: `getQuestionsForPillar(pillarSlug: string)` that returns the correct question array
- The QuestionCard, NavigationButtons, and branching logic should work unchanged since they operate on the Question interface
- When user completes final question in cyber-risk pillar, redirect to `/assessment/results` (same as governance)

**Update `src/app/api/assessment/[id]/score/route.ts`:**
- Currently hardcoded to 'family-governance' pillar in multiple places
- Accept a `pillar` field in the POST request body (default to 'family-governance' for backwards compatibility)
- When pillar is 'cyber-risk':
  - Import `cyberRiskPillar`, `cyberRiskQuestions` from `@/lib/cyber-risk/questions`
  - Import `calculateCyberRiskScore` from `@/lib/cyber-risk/scoring`
  - Filter responses by pillar='cyber-risk'
  - Use cyberRiskQuestions for visible question calculation
  - Use calculateCyberRiskScore for scoring
  - Upsert PillarScore with pillar='cyber-risk'
- When pillar is 'family-governance': keep existing behavior unchanged
- GET endpoint: accept pillar as query param, default to 'family-governance'
- Skip customization logic for cyber-risk pillar (no advisor customization yet)
  </action>
  <verify>
Run `npx tsc --noEmit` on modified files -- no type errors.
Test: Navigate to /assessment/cyber-risk/0 -- first cyber question renders.
Test: Navigate through questions -- branching logic works.
Test: POST /api/assessment/[id]/score with body `{ "pillar": "cyber-risk" }` -- returns score.
  </verify>
  <done>
Cyber risk questions render in the assessment flow at /assessment/cyber-risk/[index].
Score API calculates and persists cyber risk scores to PillarScore table.
Backwards compatibility: governance assessment unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update results page for multi-pillar display</name>
  <files>src/app/(protected)/assessment/results/page.tsx</files>
  <action>
**Update `src/app/(protected)/assessment/results/page.tsx`:**
- Currently loads score for 'family-governance' only
- Detect which pillar was just completed from the store's `currentPillar` value
- Load score for the correct pillar (pass pillar as query param to GET /api/assessment/[id]/score?pillar=X)
- When calculating score on first visit (POST), send `{ pillar: currentPillar }` in request body
- Update the hero section title to be dynamic: "Family Governance Assessment Results" or "Cyber Risk Assessment Results" based on pillar
- The ScoreDisplay, RiskDrivers, and ActionPlan components receive data props and should work unchanged
- markPillarComplete should use the actual pillar slug, not hardcoded 'family-governance'
- Add a "Continue to next assessment" button if one pillar is complete but the other is not (link back to /assessment hub)
- Keep DownloadSection and TemplateList for governance only (hide for cyber-risk, these are governance-specific report features)
  </action>
  <verify>
Run `npx tsc --noEmit` on the file -- no type errors.
Test: Complete cyber risk assessment -- results page shows "Cyber Risk Assessment Results" with correct score.
Test: Complete governance assessment -- results page still shows governance results correctly (regression).
  </verify>
  <done>
Results page dynamically displays scores for whichever pillar was just completed.
Pillar name and completion are tracked correctly.
Navigation encourages completing both assessments.
  </done>
</task>

</tasks>

<verification>
- Both pillar cards appear on /assessment hub
- Cyber risk questions render at /assessment/cyber-risk/[index]
- Score API handles both pillars via pillar parameter
- Results page correctly displays cyber risk scores
- Governance assessment flow unchanged (regression check)
- `npx tsc --noEmit` passes across all modified files
</verification>

<success_criteria>
- Family can complete cyber risk assessment end-to-end (start -> questions -> score -> results)
- Scoring produces 0-10 values matching governance scale
- Both pillars tracked independently in same Assessment record
- No regression to existing governance assessment flow
</success_criteria>

<output>
After completion, create `.planning/phases/19-cyber-risk-foundation/19-02-SUMMARY.md`
</output>
