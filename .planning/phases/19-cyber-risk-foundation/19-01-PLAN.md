---
phase: 19-cyber-risk-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/cyber-risk/types.ts
  - src/lib/cyber-risk/questions.ts
  - src/lib/cyber-risk/scoring.ts
autonomous: true

must_haves:
  truths:
    - "Cyber risk question bank exists with digital hygiene, banking security, and payment risk categories"
    - "Cyber risk pillar definition follows exact same Pillar/SubCategory/Question structure as governance"
    - "Scoring engine reuses calculatePillarScore from existing assessment/scoring.ts"
  artifacts:
    - path: "src/lib/cyber-risk/types.ts"
      provides: "Cyber-specific type re-exports and constants"
      contains: "CyberPillar"
    - path: "src/lib/cyber-risk/questions.ts"
      provides: "Complete cyber risk question bank with 4 sub-categories"
      exports: ["cyberRiskPillar", "cyberRiskQuestions"]
    - path: "src/lib/cyber-risk/scoring.ts"
      provides: "Thin wrapper around existing scoring engine for cyber pillar"
      exports: ["calculateCyberRiskScore"]
  key_links:
    - from: "src/lib/cyber-risk/questions.ts"
      to: "src/lib/assessment/types.ts"
      via: "imports Question, Pillar, SubCategory types"
      pattern: "import.*from.*assessment/types"
    - from: "src/lib/cyber-risk/scoring.ts"
      to: "src/lib/assessment/scoring.ts"
      via: "delegates to calculatePillarScore"
      pattern: "calculatePillarScore"
---

<objective>
Create the cyber risk domain data layer: question bank, pillar definition, and scoring wrapper.

Purpose: Establishes the foundation for CYBER-01 (scoring engine), FINANCE-01 (banking security), and FINANCE-02 (payment risk) by defining all cyber risk questions and the pillar structure that reuses the proven governance assessment patterns.

Output: Complete cyber risk question bank with ~20-25 questions across 4 sub-categories, pillar definition matching governance structure, and scoring wrapper.
</objective>

<execution_context>
@/Users/bwoodtalton/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bwoodtalton/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/lib/assessment/types.ts
@src/lib/assessment/questions.ts
@src/lib/assessment/scoring.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create cyber risk types and question bank</name>
  <files>src/lib/cyber-risk/types.ts, src/lib/cyber-risk/questions.ts</files>
  <action>
Create `src/lib/cyber-risk/types.ts`:
- Re-export `Question`, `Pillar`, `SubCategory`, `ScoreResult`, `RiskLevel` from `@/lib/assessment/types`
- Define `CYBER_PILLAR_ID = 'cyber-risk'` constant
- Define sub-category ID constants: `'digital-hygiene'`, `'identity-protection'`, `'banking-security'`, `'payment-risk'`

Create `src/lib/cyber-risk/questions.ts`:
- Import `Question`, `Pillar` from `@/lib/assessment/types`
- Define `cyberRiskPillar: Pillar` with id `'cyber-risk'`, slug `'cyber-risk'`, name `'Cyber Risk'`, estimatedMinutes ~15, and 4 sub-categories:

**Sub-category 1: Digital Hygiene (weight: 3)** ~6 questions covering:
- Password management practices (password manager usage, unique passwords, password length/complexity)
- Software update habits (auto-updates enabled, OS currency)
- Device security (screen lock, full-disk encryption)
Use question IDs prefixed `cyber-dh-XX`. Use 0-10 scoreMap matching governance patterns.

**Sub-category 2: Identity Protection (weight: 3)** ~5 questions covering:
- Multi-factor authentication usage across accounts
- MFA type quality (hardware key vs app vs SMS)
- Email security (phishing awareness, separate financial email)
- Social media privacy settings
Use question IDs prefixed `cyber-ip-XX`.

**Sub-category 3: Banking Security (weight: 4, FINANCE-01)** ~6 questions covering:
- How bank accounts are accessed (app vs website vs aggregator)
- Bank alert/notification setup
- Account monitoring frequency
- Authorized user management
- Wire transfer verification procedures
- Bank MFA specifically for financial accounts
Use question IDs prefixed `cyber-bs-XX`. Higher weight reflects financial focus.

**Sub-category 4: Payment Risk (weight: 3, FINANCE-02)** ~5 questions covering:
- Primary payment methods used (credit vs debit vs cash vs digital wallet)
- Online shopping security practices (virtual cards, dedicated cards)
- Recurring payment review frequency
- Peer-to-peer payment app security
- Credit monitoring enrollment
Use question IDs prefixed `cyber-pr-XX`.

Each question must have: id, text, helpText, type (single-choice/yes-no/maturity-scale), options with value/label, required: true, pillar: 'cyber-risk', subCategory matching above IDs, weight (1-5), scoreMap (0-10 scale).

Include branchingRules where logical (e.g., skip MFA type question if user says they don't use MFA).

Export `cyberRiskPillar` and `cyberRiskQuestions` (flat array of all questions) and `allCyberQuestions` alias.
  </action>
  <verify>
Run `npx tsc --noEmit src/lib/cyber-risk/types.ts src/lib/cyber-risk/questions.ts` -- no type errors.
Verify every question has all required fields by inspection.
Verify scoreMap values are 0-10 range.
Verify all questions reference valid sub-category IDs from the pillar definition.
  </verify>
  <done>
Cyber risk question bank has 20-25 questions across 4 sub-categories.
All questions follow exact same Question interface as governance.
Pillar definition matches Pillar type with proper sub-categories.
TypeScript compiles without errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create cyber risk scoring wrapper</name>
  <files>src/lib/cyber-risk/scoring.ts</files>
  <action>
Create `src/lib/cyber-risk/scoring.ts`:
- Import `calculatePillarScore` from `@/lib/assessment/scoring`
- Import `cyberRiskPillar`, `cyberRiskQuestions` from `./questions`
- Import `ScoreResult` from `@/lib/assessment/types`

Create `calculateCyberRiskScore(answers: Record<string, unknown>, visibleQuestionIds?: string[]): ScoreResult` that delegates to `calculatePillarScore(answers, cyberRiskPillar, cyberRiskQuestions, visibleQuestionIds)`.

This is intentionally a thin wrapper. The existing scoring engine already handles weighted sub-category scoring, risk level classification, and missing controls identification. Do NOT duplicate scoring logic.

Export `calculateCyberRiskScore`.
  </action>
  <verify>
Run `npx tsc --noEmit src/lib/cyber-risk/scoring.ts` -- no type errors.
Verify the function delegates to calculatePillarScore without reimplementing logic.
  </verify>
  <done>
Scoring wrapper exists and delegates to proven calculatePillarScore engine.
TypeScript compiles without errors.
No duplicated scoring logic.
  </done>
</task>

</tasks>

<verification>
- All files in `src/lib/cyber-risk/` compile with `npx tsc --noEmit`
- Question bank has 20-25 questions with consistent structure
- Every question's scoreMap produces values in 0-10 range
- Pillar definition has 4 sub-categories with weights summing appropriately
- Scoring wrapper reuses existing engine (no duplicated algorithm)
</verification>

<success_criteria>
- Cyber risk domain data layer is complete and type-safe
- Question bank covers all 4 required areas (digital hygiene, identity protection, banking security, payment risk)
- Scoring reuses proven calculatePillarScore engine
- Ready for Plan 02 to wire into assessment UI/API
</success_criteria>

<output>
After completion, create `.planning/phases/19-cyber-risk-foundation/19-01-SUMMARY.md`
</output>
