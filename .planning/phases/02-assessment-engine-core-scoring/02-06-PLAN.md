---
phase: 02-assessment-engine-core-scoring
plan: 06
type: execute
wave: 5
depends_on: ["02-04", "02-05"]
files_modified:
  - src/app/(protected)/dashboard/page.tsx
  - src/app/(protected)/assessment/page.tsx
  - src/app/(protected)/assessment/complete/page.tsx
autonomous: false

must_haves:
  truths:
    - "Dashboard shows real assessment data: in-progress or completed assessments with status"
    - "Dashboard shows completion status with timestamps and links to results or resume"
    - "Smart resume navigates user to exact next required question with visible progress context"
    - "Completed assessment shows final state with deliverable access link"
    - "Assessment responsive across desktop and tablet devices"
    - "Full end-to-end flow works: start -> answer questions -> complete -> see results -> return to dashboard"
    - "Save/resume cycle works across browser restarts: data loads from API into Zustand store on reopen"
  artifacts:
    - path: "src/app/(protected)/dashboard/page.tsx"
      provides: "Dashboard with real assessment data"
      min_lines: 60
    - path: "src/app/(protected)/assessment/page.tsx"
      provides: "Assessment hub with smart resume and server rehydration"
      min_lines: 50
    - path: "src/app/(protected)/assessment/complete/page.tsx"
      provides: "Assessment completion transition page"
      min_lines: 30
  key_links:
    - from: "src/app/(protected)/dashboard/page.tsx"
      to: "src/app/api/assessment/route.ts"
      via: "fetches user assessments list"
      pattern: "fetch.*api/assessment|assessments"
    - from: "src/app/(protected)/assessment/page.tsx"
      to: "src/lib/assessment/store.ts"
      via: "reads store for smart resume position"
      pattern: "useAssessmentStore"
    - from: "src/app/(protected)/assessment/complete/page.tsx"
      to: "src/app/api/assessment/[id]/score/route.ts"
      via: "triggers score calculation on completion"
      pattern: "fetch.*score"
    - from: "src/app/(protected)/assessment/complete/page.tsx"
      to: "src/app/(protected)/assessment/results/page.tsx"
      via: "redirects to results page after scoring"
      pattern: "router.push.*results"
---

<objective>
Update the dashboard with real assessment data, enhance the assessment hub with smart resume, create the completion transition page, and verify the full end-to-end flow including the critical save/resume cycle across browser restarts.

Purpose: Close the loop on the entire assessment experience -- from dashboard to assessment to results and back. Ensure everything works together as a cohesive, polished product. Explicitly verify that ASMT-04 (save/resume) works end-to-end with server rehydration.
Output: Updated dashboard, smart resume, completion flow, and verified end-to-end experience.
</objective>

<execution_context>
@/Users/bwoodtalton/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bwoodtalton/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-assessment-engine-core-scoring/02-RESEARCH.md
@.planning/phases/02-assessment-engine-core-scoring/02-CONTEXT.md
@.planning/phases/02-assessment-engine-core-scoring/02-03-SUMMARY.md
@.planning/phases/02-assessment-engine-core-scoring/02-04-SUMMARY.md
@.planning/phases/02-assessment-engine-core-scoring/02-05-SUMMARY.md
@src/app/(protected)/dashboard/page.tsx
@src/app/(protected)/assessment/page.tsx
@src/lib/assessment/store.ts
@src/lib/assessment/questions.ts
@src/lib/hooks/useAssessmentNavigation.ts
@src/lib/auth.ts
@src/lib/db.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dashboard with real assessment data, smart resume, and completion flow</name>
  <files>
    src/app/(protected)/dashboard/page.tsx
    src/app/(protected)/assessment/page.tsx
    src/app/(protected)/assessment/complete/page.tsx
  </files>
  <action>
    **Dashboard Page Update (src/app/(protected)/dashboard/page.tsx):**
    Replace the current placeholder "No assessments yet" content with real assessment data. This is a server component (uses `auth()` for session).

    Fetch assessments server-side using Prisma directly (server component, no API call needed):
    ```typescript
    const assessments = await prisma.assessment.findMany({
      where: { userId: session.user.id },
      include: {
        _count: { select: { responses: true } },
        scores: true,
      },
      orderBy: { updatedAt: 'desc' },
    });
    ```

    Display states:
    1. **No assessments:** Show the existing empty state with "Start Assessment" CTA (keep current design)
    2. **In-progress assessment:** Show card with:
       - "Family Governance Assessment" title
       - Status badge "In Progress"
       - Progress: "X of Y questions answered" with percentage
       - Last activity: "Last updated {relative time}" using date-fns `formatDistanceToNow`
       - "Continue Assessment" primary button linking to /assessment
       - Per user decision: "Comprehensive completion status showing overall progress, pillar-by-pillar completion"
    3. **Completed assessment:** Show card with:
       - "Family Governance Assessment" title
       - Status badge "Completed" (green)
       - Score and risk level from PillarScore
       - "Completed on {date}" using date-fns `format`
       - "View Results" primary button linking to /assessment/results
       - "Start New Assessment" secondary button
       - Per user decision: "final completion state with timestamps and deliverable access"

    Keep the Account Settings card (second column) as-is.

    Import `prisma` from `@/lib/db` and `formatDistanceToNow, format` from `date-fns`. Calculate total question count from the allQuestions export.

    **Assessment Hub Enhancement (src/app/(protected)/assessment/page.tsx):**
    Enhance the hub page created in Plan 03 with smart resume logic.

    INTEGRATION NOTE: Read the existing Plan 03 hub page implementation first. The Plan 03 hub page has a modular structure with header, progress, pillar cards, and CTA sections. This task enhances the CTA section and adds the "Welcome Back" banner. Do NOT rewrite the entire page -- import existing components and add to them.

    Before modifying, verify that the Plan 03 hub page:
    - Has the server rehydration flow (loadFromServer on mount)
    - Uses isHydrated to gate rendering
    - Has the PillarCard and ProgressBar components

    If the Plan 03 implementation differs from expectations, adapt the enhancements to fit the actual structure rather than forcing a predefined shape.

    Per user decision: "Smart resume that recognizes in-progress state and takes users to exact next required question with visible progress context."

    When an in-progress assessment is detected (after server rehydration completes):
    1. Show a prominent "Welcome Back" banner at the top
    2. Display: "You've completed X of Y questions. Pick up where you left off."
    3. Show the pillar card(s) with current progress highlighted
    4. "Continue" CTA navigates directly to `/assessment/family-governance/{nextQuestionIndex}` based on stored position
    5. Show last saved time: "Last saved {relative time}"

    The "next required question" logic: Read currentQuestionIndex from Zustand store (which was rehydrated from server in Plan 03). Use `getVisibleQuestions` to get the ordered question list. Find the first unanswered question at or after the stored index. Navigate there.

    Also add ability to review completed sections: if pillar is completed, show "Review Answers" button that allows re-entry at question 0 for that pillar.

    **Completion Transition Page (src/app/(protected)/assessment/complete/page.tsx):**
    Client component. URL: `/assessment/complete`.

    This is a brief transition page shown after the user answers the last question. Flow:
    1. Show "Calculating your results..." with a professional loading state
    2. Trigger POST /api/assessment/[id]/score to calculate scores
    3. On success: automatically redirect to /assessment/results after 2 seconds
    4. On error: show error message with "Try Again" button

    This exists to create a clean transition moment and handle the async score calculation. The "Complete Section" button in the question flow (Plan 04) navigates here, and this page redirects to /assessment/results (created in Plan 05). Both Plan 04 and Plan 05 must be complete before this page works correctly.

    **Responsive Design:**
    Ensure all components from Plans 03-06 work on desktop (1200px+) and tablet (768px+) per requirement UI-05. Key responsive points:
    - Dashboard: 2-column grid on desktop, stack on tablet/mobile
    - Assessment hub: Pillar cards stack on mobile
    - Question flow: max-w-2xl centered (naturally responsive)
    - Results: Score display and breakdown stack on mobile
    - Navigation buttons: full-width on mobile with adequate touch targets
  </action>
  <verify>
    - `npm run build` passes
    - Dashboard shows different states: no assessment, in-progress, completed
    - Assessment hub smart resume navigates to correct question
    - Assessment hub "Welcome Back" banner appears for in-progress assessments
    - Hub page still performs server rehydration (Plan 03 behavior preserved)
    - Completion page triggers score calculation and redirects to /assessment/results
    - Responsive layout works at 768px and 1200px viewport widths
  </verify>
  <done>
    Dashboard displays real assessment data with status, progress, and timestamps. Smart resume navigates users to their exact position. Completion transition handles score calculation and redirects to results. Hub page enhancements integrate cleanly with Plan 03 base. All pages responsive on desktop and tablet.
  </done>
</task>

<task type="auto">
  <name>Task 2: End-to-end verification including save/resume persistence cycle</name>
  <files>
    src/lib/hooks/useAssessmentNavigation.ts
  </files>
  <action>
    Perform a build verification and smoke test of the complete end-to-end assessment flow, with particular attention to ASMT-04 (save/resume persistence):

    1. Run `npm run build` and verify all pages compile successfully
    2. Verify all dynamic routes are registered in build output: /assessment, /assessment/[pillarSlug]/[questionIndex], /assessment/results, /assessment/complete
    3. Verify all API routes are registered: /api/assessment, /api/assessment/[id], /api/assessment/[id]/responses, /api/assessment/[id]/score
    4. Run `npx tsc --noEmit` to verify TypeScript is clean
    5. Verify no circular dependencies in the assessment module (store imports types, questions imports types, scoring imports types+questions -- no cycles)
    6. Check that the question flow navigation from the last question correctly routes to /assessment/complete (update the navigation hook if it routes to results directly -- it should go through the completion page first)
    7. Verify the completion page (/assessment/complete) redirects to /assessment/results (which exists from Plan 05)

    **ASMT-04 Persistence Verification:**
    8. Verify that the Zustand store's `loadFromServer` action exists and accepts `ServerAssessmentData`
    9. Verify that GET /api/assessment/[id] returns responses array (needed for rehydration)
    10. Verify that the assessment hub page calls `loadFromServer` when assessmentId exists (trace the code path: mount -> check assessmentId -> fetch API -> call loadFromServer)
    11. Verify the hub page gates rendering on `isHydrated` (shows loading state until server data arrives)

    If the navigation hook (useAssessmentNavigation from Plan 04) routes directly to /assessment/results on last question, update it to route to /assessment/complete instead. This is the only file modification needed in this task -- the completion page (Task 1) triggers score calculation before redirecting to results.
  </action>
  <verify>
    - `npm run build` passes with zero errors
    - `npx tsc --noEmit` passes
    - All assessment routes present in build output
    - Last question navigation goes to /assessment/complete (not /assessment/results directly)
    - Completion page redirects to /assessment/results (route exists from Plan 05)
    - Store has loadFromServer action
    - Hub page fetches from API and calls loadFromServer on mount
    - Hub page shows loading skeleton until isHydrated is true
  </verify>
  <done>
    Full build verification passes. All routes registered. TypeScript clean. Navigation flow confirmed: questions -> complete -> results. ASMT-04 persistence chain verified: Zustand store -> loadFromServer -> API fetch -> server rehydration -> smart resume. Ready for human verification.
  </done>
</task>

</tasks>

<checkpoint>
After both tasks complete, the user should verify the full end-to-end flow with particular attention to save/resume:

1. Navigate to http://localhost:3000 and sign in
2. Dashboard should show "No assessments yet" or existing assessment
3. Click "Start Assessment" to begin
4. Verify: Assessment hub shows Family Governance pillar with description and time estimate
5. Click to start assessment
6. Verify: First question appears one-per-screen with card-based answer options
7. Select an answer -- verify it highlights and auto-saves
8. Click Next -- verify next question appears with progress bar updating
9. Click Back -- verify previous answer is preserved
10. Try clicking Next without answering a required question -- verify validation message
11. Answer 5-10 questions, then close the browser tab
12. **CRITICAL ASMT-04 test:** Return to http://localhost:3000/assessment -- verify:
    a. "Welcome Back" banner appears
    b. Progress shows correct count of answered questions
    c. Click "Continue" -- verify it navigates to the correct next question
    d. Previously answered questions still have their answers
13. **CRITICAL persistence test:** Open browser DevTools -> Application -> Local Storage -> delete 'belvedere-assessment' key
14. Refresh the page at /assessment -- verify data STILL loads from server (not just localStorage)
15. Complete all questions (or enough to pass 50% threshold)
16. Verify: Completion page shows "Calculating your results..."
17. Verify: Results page shows score, risk level, category breakdown, risk drivers, action plan
18. Return to dashboard -- verify "Completed" status with score and "View Results" link
19. Test on tablet-width viewport (768px) -- verify layout adjusts properly
</checkpoint>

<verification>
- Full end-to-end flow: dashboard -> start -> questions -> complete -> results -> dashboard
- Dashboard shows real assessment data (not placeholder)
- Smart resume works after closing/reopening browser
- **ASMT-04: Resume works even after clearing localStorage (server rehydration)**
- Score calculation produces meaningful results
- Results page shows professional advisory-tone output
- Responsive on desktop (1200px+) and tablet (768px+)
- Auto-save prevents data loss
- Branching logic skips irrelevant questions
- Completion page correctly redirects to results (Plan 05 dependency satisfied)
</verification>

<success_criteria>
1. Dashboard shows assessment status: not started, in-progress (with progress), completed (with score)
2. Smart resume navigates to exact next required question
3. Save/resume works across browser restarts (server rehydration, not just localStorage)
4. Save/resume works even when localStorage is cleared (server is source of truth)
5. Completion page calculates and persists scores before showing results
6. Completion page redirects to /assessment/results (which exists from Plan 05)
7. All pages responsive on desktop and tablet
8. Human verification confirms complete end-to-end flow works
9. Assessment feels like "structured 15-minute guided interview" (per user specification)
</success_criteria>

<output>
After completion, create `.planning/phases/02-assessment-engine-core-scoring/02-06-SUMMARY.md`
</output>
