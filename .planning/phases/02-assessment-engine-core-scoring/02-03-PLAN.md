---
phase: 02-assessment-engine-core-scoring
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - src/app/(protected)/assessment/[pillarSlug]/[questionIndex]/page.tsx
  - src/components/assessment/QuestionCard.tsx
  - src/components/assessment/AnswerOptions.tsx
  - src/components/assessment/NavigationButtons.tsx
  - src/components/assessment/InlineHelp.tsx
  - src/lib/hooks/useAutoSave.ts
  - src/lib/hooks/useAssessmentNavigation.ts
autonomous: true

must_haves:
  truths:
    - "User sees one question per screen with clear plain-language prompt and card-based answer options"
    - "User can select answers via clean card-based selections for yes/no and maturity questions"
    - "User can navigate back and forward between questions without losing answers"
    - "User can skip optional questions with reduced accuracy note"
    - "Hard validation blocks progression on required questions with professional messaging"
    - "Answers auto-save after selection with debounced server persistence"
    - "User sees inline help text and can expand Learn More sections"
    - "User sees section-level progress bar during question flow"
  artifacts:
    - path: "src/app/(protected)/assessment/[pillarSlug]/[questionIndex]/page.tsx"
      provides: "Dynamic question route rendering current question"
      min_lines: 40
    - path: "src/components/assessment/QuestionCard.tsx"
      provides: "Single question display with answer rendering"
      exports: ["QuestionCard"]
    - path: "src/components/assessment/AnswerOptions.tsx"
      provides: "Card-based answer options for all question types"
      exports: ["SingleChoiceCards", "YesNoCards", "MaturityScale", "NumericInput", "ShortTextInput"]
    - path: "src/components/assessment/NavigationButtons.tsx"
      provides: "Back, Next, Skip navigation controls"
      exports: ["NavigationButtons"]
    - path: "src/components/assessment/InlineHelp.tsx"
      provides: "Help text, Learn More expandable, tooltips"
      exports: ["InlineHelp"]
    - path: "src/lib/hooks/useAutoSave.ts"
      provides: "Debounced auto-save with queued mutations"
      exports: ["useAutoSave"]
    - path: "src/lib/hooks/useAssessmentNavigation.ts"
      provides: "Navigation logic with branching support"
      exports: ["useAssessmentNavigation"]
  key_links:
    - from: "src/app/(protected)/assessment/[pillarSlug]/[questionIndex]/page.tsx"
      to: "src/components/assessment/QuestionCard.tsx"
      via: "renders current question"
      pattern: "QuestionCard"
    - from: "src/components/assessment/QuestionCard.tsx"
      to: "src/components/assessment/AnswerOptions.tsx"
      via: "renders appropriate answer component by question type"
      pattern: "SingleChoiceCards|YesNoCards|MaturityScale"
    - from: "src/lib/hooks/useAutoSave.ts"
      to: "src/app/api/assessment/[id]/responses/route.ts"
      via: "POSTs answer to save endpoint"
      pattern: "fetch.*responses"
    - from: "src/lib/hooks/useAssessmentNavigation.ts"
      to: "src/lib/assessment/branching.ts"
      via: "uses branching rules to determine next question"
      pattern: "getNextQuestion|getVisibleQuestions"
    - from: "src/app/(protected)/assessment/[pillarSlug]/[questionIndex]/page.tsx"
      to: "src/lib/assessment/store.ts"
      via: "reads/writes current position and answers"
      pattern: "useAssessmentStore"
---

<objective>
Build the one-question-per-screen assessment flow with card-based answer selection, auto-save, navigation with branching support, and inline help. This is the core TurboTax-style guided experience.

Purpose: Create the primary user interaction -- the question-by-question guided assessment that feels like a structured interview, not a survey.
Output: Working question flow with all answer types, navigation, auto-save, branching, progress tracking, and inline help.
</objective>

<execution_context>
@/Users/bwoodtalton/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bwoodtalton/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-assessment-engine-core-scoring/02-RESEARCH.md
@.planning/phases/02-assessment-engine-core-scoring/02-CONTEXT.md
@.planning/phases/02-assessment-engine-core-scoring/02-01-SUMMARY.md
@.planning/phases/02-assessment-engine-core-scoring/02-02-SUMMARY.md
@src/lib/assessment/types.ts
@src/lib/assessment/questions.ts
@src/lib/assessment/branching.ts
@src/lib/assessment/store.ts
@src/components/assessment/PillarCard.tsx
@src/components/assessment/ProgressBar.tsx
@src/app/api/assessment/[id]/responses/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: QuestionCard component with answer type renderers and inline help</name>
  <files>
    src/components/assessment/QuestionCard.tsx
    src/components/assessment/AnswerOptions.tsx
    src/components/assessment/InlineHelp.tsx
  </files>
  <action>
    **AnswerOptions (src/components/assessment/AnswerOptions.tsx):**
    Client component. Create answer renderers for each QuestionType. All use shadcn Card components for a premium card-based selection feel (per user decision: "Clean card-based selections").

    `SingleChoiceCards`: Renders options as clickable cards with radio-style selection. Each card shows option label and optional description. Selected card gets a distinct border/background (e.g., zinc-900 border, light blue-50 bg). One card selected at a time. Props: `{ options: QuestionOption[], value: string | null, onChange: (value: string) => void }`.

    `YesNoCards`: Two large cards side-by-side: "Yes" and "No". Clean, prominent. Same selection styling as SingleChoiceCards. Props: `{ value: string | null, onChange: (value: string) => void }`.

    `MaturityScale`: Renders 3-5 maturity level options as stacked cards (e.g., "No formal process" / "Informal, case-by-case" / "Documented but not always followed" / "Formal, consistently applied"). Each card has the level label and a short description. Visually indicates progression (subtle left border color getting stronger). Props: `{ options: QuestionOption[], value: string | null, onChange: (value: string) => void }`.

    `NumericInput`: Clean numeric input field using shadcn Input. Shows label and optional unit suffix. Props: `{ value: number | null, onChange: (value: number) => void, min?: number, max?: number, placeholder?: string }`.

    `ShortTextInput`: Clean text input/textarea using shadcn Input or Textarea. Character limit indicator. Props: `{ value: string, onChange: (value: string) => void, maxLength?: number, placeholder?: string }`.

    All components use `'use client'` and controlled component pattern. No form submission -- answer changes are communicated via onChange callback. This keeps the auto-save mechanism in the parent.

    **InlineHelp (src/components/assessment/InlineHelp.tsx):**
    Client component providing help per user decisions:
    - Short inline context sentence below the question (always visible, subtle text)
    - Optional "Learn More" expandable section (collapsible, not shown by default)
    - Subtle tooltips for defined terms (wrap specific words in question text)

    Props: `{ helpText?: string, learnMore?: string }`.

    Use a simple disclosure pattern (button to expand/collapse "Learn More" content). Use shadcn Tooltip for term definitions if needed.

    **QuestionCard (src/components/assessment/QuestionCard.tsx):**
    Client component. The main wrapper for a single question screen.

    Props: `{ question: Question, currentAnswer: unknown, onAnswer: (answer: unknown) => void, onSkip?: () => void }`.

    Layout (per user decision: "one question per screen with minimal visual clutter"):
    - Question text as prominent heading (text-xl or text-2xl, font-semibold)
    - InlineHelp immediately below
    - Answer component appropriate to question.type (switch on type to render correct AnswerOptions sub-component)
    - If question is not required: subtle "Skip this question" link at bottom with note "(answering improves accuracy)"
    - Clean, centered layout (max-w-2xl mx-auto) with generous spacing
    - No question number display (per research anti-pattern). No "Question 5 of 80".

    The QuestionCard does NOT handle navigation -- that's the parent page's job. It only handles answer selection and skip.

    Validation: Use React Hook Form with Zod resolver for the current question. Schema created dynamically from question.type and question.required. Validate on submission attempt (mode: 'onSubmit'), not while typing (per research: avoid premature inline validation). If required and no answer selected, show gentle error: "Please select an answer to continue" (per user decision: "professional messaging").
  </action>
  <verify>
    - `npm run build` passes
    - All components export correctly
    - QuestionCard renders all 5 answer types (single-choice, yes-no, maturity-scale, numeric, short-text)
    - InlineHelp shows helpText and toggles learnMore
    - No hardcoded question text in components (all from props)
  </verify>
  <done>
    QuestionCard component renders any question type with card-based answer options, inline help, and skip capability for optional questions. Premium visual styling with minimal clutter.
  </done>
</task>

<task type="auto">
  <name>Task 2: Auto-save hook, navigation logic, and dynamic question route page</name>
  <files>
    src/lib/hooks/useAutoSave.ts
    src/lib/hooks/useAssessmentNavigation.ts
    src/app/(protected)/assessment/[pillarSlug]/[questionIndex]/page.tsx
    src/components/assessment/NavigationButtons.tsx
  </files>
  <action>
    **useAutoSave Hook (src/lib/hooks/useAutoSave.ts):**
    Implements debounced auto-save with TanStack Query mutations (per research: "queued mutations to guarantee FIFO execution and prevent race conditions").

    ```typescript
    export function useAutoSave(assessmentId: string | null) {
      // useMutation with onMutate for optimistic updates
      // Debounce with use-debounce (1000ms)
      // On answer change: update Zustand store immediately, debounce API call
      // On error: show toast "Failed to save, retrying..." and retry once
      // On success: update lastSaved timestamp in store
      // Returns: { saveAnswer, isSaving, lastSaved }
    }
    ```

    The hook takes assessmentId and returns a `saveAnswer(questionId, pillar, subCategory, answer, skipped?)` function. Internally:
    1. Immediately updates Zustand store (instant UI feedback)
    2. Debounces the actual API POST to /api/assessment/[id]/responses
    3. Uses TanStack Query mutation with optimistic update pattern from research
    4. Shows toast on save error (react-hot-toast)

    **useAssessmentNavigation Hook (src/lib/hooks/useAssessmentNavigation.ts):**
    Navigation logic incorporating branching rules.

    ```typescript
    export function useAssessmentNavigation(pillarSlug: string) {
      // Returns: { currentQuestion, goNext, goBack, canGoBack, isLastQuestion, progress }
    }
    ```

    Internal logic:
    1. Get visible questions using `getVisibleQuestions(answers, allQuestions)` from branching.ts
    2. Current question determined by URL param (questionIndex)
    3. `goNext()`: Find next visible question, navigate to its index. If last question in pillar, navigate to pillar completion / results.
    4. `goBack()`: Find previous visible question, navigate back. Cannot go before first question.
    5. `canGoBack`: false if at first question
    6. `isLastQuestion`: true if current is last visible question
    7. `progress`: { answered: number, total: number (visible questions only), percentage: number }

    Navigation uses `router.push(/assessment/${pillarSlug}/${nextIndex})`. Update Zustand store's currentPillar and currentQuestionIndex on navigation.

    **NavigationButtons (src/components/assessment/NavigationButtons.tsx):**
    Client component. Props: `{ onBack: () => void, onNext: () => void, canGoBack: boolean, isLastQuestion: boolean, isValid: boolean, isSaving: boolean }`.

    Layout:
    - Back button (left, outline variant, disabled if canGoBack is false)
    - Next button (right, primary variant, shows "Complete Section" if isLastQuestion)
    - If isSaving: subtle "Saving..." indicator near the buttons
    - If not isValid and Next is clicked: validation message from QuestionCard's form

    Per user decision: hard validation blocks progression for required questions. If the question is required and unanswered, Next button click triggers form validation (which shows the error message).

    **Dynamic Question Route (src/app/(protected)/assessment/[pillarSlug]/[questionIndex]/page.tsx):**
    Client component ('use client'). The core question flow page.

    URL pattern: `/assessment/family-governance/0`, `/assessment/family-governance/1`, etc. The questionIndex is the index into the visible questions array (after branching).

    On mount:
    1. Parse pillarSlug and questionIndex from params
    2. Use `useAssessmentNavigation(pillarSlug)` to get current question and navigation functions
    3. Use `useAutoSave(assessmentId)` to get save function
    4. Read existing answer from Zustand store

    Render:
    - SectionProgress at top (from Plan 02's ProgressBar component) showing progress within current sub-category/pillar
    - QuestionCard with the current question, existing answer, and onChange handler
    - NavigationButtons at bottom

    On answer selection:
    1. QuestionCard calls onAnswer
    2. Auto-save hook saves to store + debounces API call
    3. UI immediately reflects selected answer (optimistic)

    On Next:
    1. Validate current question (if required, must have answer)
    2. If valid: call goNext() which navigates to next visible question
    3. If last question: navigate to pillar results or assessment completion

    On Back:
    1. Call goBack() which navigates to previous question
    2. Previous answer is still in Zustand store (displayed automatically)

    Per user decision: "Users can go back and change answers within sections and after completion with recalculation and updated risk scores." Changing an answer on a revisited question triggers auto-save and score recalculation (handled by scoring engine in Plan 04).

    IMPORTANT: The page must handle browser back/forward navigation gracefully. Since state is in Zustand (not URL), the URL serves as the navigation index.
  </action>
  <verify>
    - `npm run build` passes
    - Dynamic route [pillarSlug]/[questionIndex] registered in build output
    - Auto-save hook debounces API calls (1000ms)
    - Navigation respects branching rules (skips hidden questions)
    - Back button disabled on first question
    - Required questions block progression when unanswered
    - Optional questions show skip link
  </verify>
  <done>
    Complete question flow works: user navigates through questions one at a time, selects card-based answers, sees inline help, answers auto-save with debounce, navigation respects branching rules, required validation blocks progression with gentle messaging, back/forward navigation preserves answers.
  </done>
</task>

</tasks>

<verification>
- `npm run build` passes clean
- Question flow accessible at /assessment/family-governance/0
- All 5 answer types render correctly in QuestionCard
- Auto-save persists answers to API (visible in network tab or server logs)
- Navigation skips branched-out questions
- Progress bar updates as questions are answered
- Back button navigates to previous question with answer preserved
- Required validation blocks empty progression
- Optional questions can be skipped
</verification>

<success_criteria>
1. One question per screen with card-based answer selections (per locked decision)
2. All 5 question types render with appropriate UI components
3. Inline help text visible; Learn More expandable
4. Auto-save after every answer with debounced server persistence
5. Navigation back/forward works with branching logic (skip irrelevant questions)
6. Hard validation on required questions; skip allowed on optional with accuracy note
7. Section progress bar visible during question flow (not "Question X of 80")
8. Premium visual experience -- clean, minimal, professional advisory tone
</success_criteria>

<output>
After completion, create `.planning/phases/02-assessment-engine-core-scoring/02-03-SUMMARY.md`
</output>
