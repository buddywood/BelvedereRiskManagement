---
phase: 05-profile-foundation
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/app/(protected)/profiles/page.tsx
  - src/components/profiles/ProfileForm.tsx
  - src/components/profiles/MemberCard.tsx
  - src/components/profiles/RoleSelector.tsx
  - src/app/(protected)/layout.tsx
autonomous: true

must_haves:
  truths:
    - "User can navigate to profiles page from main nav"
    - "User can add a new household member with name, age, occupation, phone, email, relationship, and governance roles"
    - "User can see all household members displayed as cards with their information"
    - "User can edit an existing household member's profile"
    - "User can delete a household member with confirmation"
    - "User can mark a member as extended family (non-resident)"
    - "User can assign multiple governance roles to a household member"
  artifacts:
    - path: "src/app/(protected)/profiles/page.tsx"
      provides: "Profile management page with member list and add/edit modal"
      min_lines: 50
    - path: "src/components/profiles/ProfileForm.tsx"
      provides: "React Hook Form + Zod form for creating/editing household members"
      exports: ["ProfileForm"]
    - path: "src/components/profiles/MemberCard.tsx"
      provides: "Card component displaying household member info with edit/delete actions"
      exports: ["MemberCard"]
    - path: "src/components/profiles/RoleSelector.tsx"
      provides: "Multi-select component for governance role assignment"
      exports: ["RoleSelector"]
  key_links:
    - from: "src/app/(protected)/profiles/page.tsx"
      to: "src/lib/actions/profile-actions.ts"
      via: "Server Action imports for CRUD"
      pattern: "import.*from.*profile-actions"
    - from: "src/components/profiles/ProfileForm.tsx"
      to: "src/lib/schemas/profile.ts"
      via: "Zod schema for form validation"
      pattern: "zodResolver.*householdMemberSchema"
    - from: "src/app/(protected)/layout.tsx"
      to: "/profiles"
      via: "Navigation link in header"
      pattern: "href.*profiles"
---

<objective>
Build the UI layer for household member profile management: form components, member cards, and the profiles page.

Purpose: Delivers the user-facing interface for all 6 PROFILE requirements (create, edit, delete, relationships, roles, extended family).
Output: Fully functional /profiles page where users manage their household.
</objective>

<execution_context>
@/Users/bwoodtalton/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bwoodtalton/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-profile-foundation/05-RESEARCH.md
@.planning/phases/05-profile-foundation/05-01-SUMMARY.md
@src/app/(protected)/layout.tsx
@src/components/ui/button.tsx
@src/components/ui/card.tsx
@src/components/ui/input.tsx
@src/components/ui/badge.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Profile form and role selector components</name>
  <files>src/components/profiles/ProfileForm.tsx, src/components/profiles/RoleSelector.tsx</files>
  <action>
1. Create src/components/profiles/RoleSelector.tsx:
- 'use client' directive
- Props: value: GovernanceRole[], onChange: (roles: GovernanceRole[]) => void
- Import GOVERNANCE_ROLE_LABELS from @/lib/schemas/profile
- Render a grid of toggle buttons (2 cols on mobile, 3 on desktop) for each governance role
- Each button shows the role label, uses variant="outline" when unselected, variant="default" when selected
- Clicking toggles the role in/out of the array
- Use Badge component from shadcn/ui for selected roles display

2. Create src/components/profiles/ProfileForm.tsx:
- 'use client' directive
- Props: defaultValues? (for edit mode), onSubmit: (data) => Promise<void>, onCancel: () => void, isSubmitting: boolean
- Use React Hook Form with zodResolver(householdMemberSchema)
- Import RELATIONSHIP_LABELS, GOVERNANCE_ROLE_LABELS from @/lib/schemas/profile
- Form fields layout (responsive grid):
  - Full Name (Input, required) - full width
  - Age (Input type="number", optional) - half width
  - Occupation (Input, optional) - half width
  - Phone (Input, optional) - half width
  - Email (Input type="email", optional) - half width
  - Relationship (select dropdown using native select element styled with existing Input classes, required) - full width
  - Is Resident (checkbox with label "Lives in household" - unchecked = extended family per PROFILE-06) - full width
  - Governance Roles (RoleSelector component, integrated via Controller from react-hook-form) - full width
  - Notes (Textarea, optional) - full width
- Submit and Cancel buttons at bottom
- Show field-level validation errors from formState.errors
- When defaultValues provided, pre-populate all fields (edit mode)
  </action>
  <verify>npx tsc --noEmit passes. Components export correctly.</verify>
  <done>ProfileForm renders all household member fields with React Hook Form + Zod validation. RoleSelector allows multi-select of governance roles. Both components handle create and edit modes.</done>
</task>

<task type="auto">
  <name>Task 2: Member card, profiles page, and navigation</name>
  <files>src/components/profiles/MemberCard.tsx, src/app/(protected)/profiles/page.tsx, src/app/(protected)/layout.tsx</files>
  <action>
1. Create src/components/profiles/MemberCard.tsx:
- 'use client' directive
- Props: member (HouseholdMember from Prisma), onEdit: (member) => void, onDelete: (id: string) => void
- Use Card component from shadcn/ui
- Display: fullName as card title, relationship as Badge (use RELATIONSHIP_LABELS for display), age and occupation on same line
- Show contact info (phone, email) if present
- Show governance roles as Badge components (variant="secondary") if any assigned
- Show "Extended Family" badge if isResident is false
- Edit button (pencil icon) and Delete button (trash icon) in card footer
- Delete button should use a confirm dialog (window.confirm is fine for now)

2. Create src/app/(protected)/profiles/page.tsx:
- This is a Server Component (no 'use client')
- Import getHouseholdMembers from @/lib/actions/profile-actions
- Fetch members on page load via getHouseholdMembers()
- Import a client wrapper component (define inline or as separate component) that manages:
  - State: showForm (boolean), editingMember (member | null)
  - "Add Household Member" button that shows the ProfileForm
  - Member cards grid (responsive: 1 col mobile, 2 cols md, 3 cols lg)
  - When edit clicked on MemberCard, set editingMember and show ProfileForm with defaultValues
  - When delete clicked on MemberCard, call deleteHouseholdMember server action
  - On form submit: call createHouseholdMember or updateHouseholdMember server action
  - Use useRouter().refresh() after mutations to reload server data
  - Show toast notification (react-hot-toast) on success/error
- Page heading: "Household Members" with subtitle "Manage your family members and their governance roles"
- Empty state: when no members exist, show a centered message encouraging the user to add their first member

3. Update src/app/(protected)/layout.tsx:
- Add "Profiles" nav link between "Assessment" and "Settings" in the nav section
- Link href="/profiles"
- Follow existing Button asChild variant="ghost" pattern
  </action>
  <verify>npx tsc --noEmit passes. Navigate to /profiles in browser - page loads with empty state. Add a member - form appears. After submission, member card appears in grid.</verify>
  <done>Profiles page displays household members as cards, supports add/edit/delete flows, shows governance roles and relationship info. Navigation link added to protected layout. Empty state guides new users.</done>
</task>

</tasks>

<verification>
- /profiles page loads for authenticated users
- "Profiles" link appears in main navigation
- User can add a household member with all profile fields
- User can edit an existing household member
- User can delete a household member with confirmation
- Governance roles display as badges on member cards
- Extended family members show "Extended Family" badge
- Form validation prevents invalid submissions
- All PROFILE-01 through PROFILE-06 requirements satisfied
</verification>

<success_criteria>
Complete profile management UI: users can navigate to /profiles, add household members with full profile information (PROFILE-01), specify relationships (PROFILE-02), assign governance roles (PROFILE-03), edit profiles (PROFILE-04), remove members (PROFILE-05), and track extended family with isResident flag (PROFILE-06).
</success_criteria>

<output>
After completion, create `.planning/phases/05-profile-foundation/05-02-SUMMARY.md`
</output>
