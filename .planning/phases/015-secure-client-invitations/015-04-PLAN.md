---
phase: 015-secure-client-invitations
plan: 04
type: execute
wave: 3
depends_on: ["015-02", "015-03"]
files_modified:
  - src/app/(protected)/advisor/invitations/page.tsx
  - src/components/advisor/invitations/InviteClientForm.tsx
  - src/components/advisor/invitations/InvitationTable.tsx
  - src/components/advisor/invitations/InvitationStatusBadge.tsx
  - src/app/(protected)/advisor/page.tsx
  - src/app/(protected)/advisor/settings/page.tsx
autonomous: false

must_haves:
  truths:
    - "Advisor can send invitation from a form with client email, name, and personal message"
    - "Advisor can see all their invitations with status (sent, opened, registered, expired)"
    - "Advisor can resend expired or unopened invitations"
    - "Advisor can manually expire active invitations"
    - "Advisor can upload logo URL and customize their branding in settings"
    - "Invitation management is accessible from the advisor portal"
  artifacts:
    - path: "src/app/(protected)/advisor/invitations/page.tsx"
      provides: "Invitation management page with form and table"
      min_lines: 30
    - path: "src/components/advisor/invitations/InviteClientForm.tsx"
      provides: "Form to create and send new invitation"
      min_lines: 50
    - path: "src/components/advisor/invitations/InvitationTable.tsx"
      provides: "Table of all advisor invitations with actions"
      min_lines: 50
    - path: "src/components/advisor/invitations/InvitationStatusBadge.tsx"
      provides: "Status badge component for invitation states"
      exports: ["InvitationStatusBadge"]
    - path: "src/app/(protected)/advisor/settings/page.tsx"
      provides: "Advisor settings page with logo URL and branding config"
  key_links:
    - from: "src/components/advisor/invitations/InviteClientForm.tsx"
      to: "src/lib/actions/invitations.ts"
      via: "form action calls sendInvitation server action"
      pattern: "sendInvitation"
    - from: "src/components/advisor/invitations/InvitationTable.tsx"
      to: "src/lib/actions/invitations.ts"
      via: "resendInvitationAction and expireInvitationAction"
      pattern: "(resendInvitationAction|expireInvitationAction)"
    - from: "src/app/(protected)/advisor/invitations/page.tsx"
      to: "src/lib/actions/invitations.ts"
      via: "getInvitationsAction for initial data load"
      pattern: "getInvitationsAction"
---

<objective>
Build the advisor invitation management UI: send new invitations, view status, resend/expire, and configure branding.

Purpose: The advisor-facing interface that completes the invitation workflow. Advisors need to send, track, and manage client invitations from their portal.
Output: Invitation management page, invite form, invitation table with actions, branding settings.
</objective>

<execution_context>
@/Users/bwoodtalton/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bwoodtalton/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/015-secure-client-invitations/015-01-SUMMARY.md
@.planning/phases/015-secure-client-invitations/015-02-SUMMARY.md
@.planning/phases/015-secure-client-invitations/015-03-SUMMARY.md
@src/app/(protected)/advisor/page.tsx
@src/components/advisor/ClientCard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create invitation management page with form and table</name>
  <files>src/app/(protected)/advisor/invitations/page.tsx, src/components/advisor/invitations/InviteClientForm.tsx, src/components/advisor/invitations/InvitationTable.tsx, src/components/advisor/invitations/InvitationStatusBadge.tsx</files>
  <action>
**src/components/advisor/invitations/InvitationStatusBadge.tsx:**
Client component. Render colored badge based on InvitationStatus:
- SENT: blue/info style, text "Sent"
- OPENED: amber/warning style, text "Opened"
- REGISTERED: green/success style, text "Registered"
- EXPIRED: gray/muted style, text "Expired"
Use existing Badge component or simple styled span matching codebase conventions.

**src/components/advisor/invitations/InviteClientForm.tsx:**
Client component ("use client"). Form to send a new invitation:
- Fields: clientEmail (required), clientName (optional), personalMessage (textarea, optional, with default template pre-filled)
- Per locked decision: guided personalization with customizable greeting, personal note, call-to-action
- Use React Hook Form + Zod resolver with createInvitationSchema from Plan 02
- Submit calls `sendInvitation` server action via form action or fetch
- Show loading state during submission
- Show success toast (use existing toast/sonner pattern) with "Invitation sent to {email}"
- Show error inline if validation fails
- Reset form on success
- Personal message textarea should have a character counter showing remaining of 2000 chars

**src/components/advisor/invitations/InvitationTable.tsx:**
Client component ("use client"). Table displaying all advisor invitations:
- Columns: Client Name, Email, Status (badge), Sent Date, Expires, Actions
- Actions column: "Resend" button (visible when status is SENT or EXPIRED and not at resend limit), "Expire" button (visible when status is SENT or OPENED)
- Resend calls resendInvitationAction, shows toast on success
- Expire calls expireInvitationAction with confirmation dialog, shows toast on success
- Empty state: "No invitations yet. Send your first invitation above."
- Use existing table styling patterns from the codebase (check if there's a Table component or use simple HTML table with Tailwind)
- Sort by date descending (newest first)
- Show relative time for sent date ("2 hours ago", "3 days ago")

**src/app/(protected)/advisor/invitations/page.tsx:**
Server component. The invitations management page:
- Call getInvitationsAction() to load initial invitation data
- Layout: Page header "Client Invitations" with description text
- InviteClientForm at top in a card
- Section divider
- InvitationTable below with the invitation data
- Follow existing advisor page patterns (space-y-8, editorial-kicker, heading styles from advisor/page.tsx)
  </action>
  <verify>Run `npx tsc --noEmit`. Navigate to /advisor/invitations in browser to verify page renders. Check that form submits and table displays (may need seed data).</verify>
  <done>Invitation management page renders with form and table. Form validates inputs and calls sendInvitation. Table shows invitations with status badges and action buttons. Resend and expire actions work with confirmation.</done>
</task>

<task type="auto">
  <name>Task 2: Add portal navigation and advisor branding settings</name>
  <files>src/app/(protected)/advisor/page.tsx, src/app/(protected)/advisor/settings/page.tsx</files>
  <action>
**src/app/(protected)/advisor/page.tsx:**
Add a navigation card to the advisor portal page linking to the invitations management page. Follow the existing pattern of cards on this page:
- Add card with title "Client Invitations", description "Send and manage client invitations", Link to "/advisor/invitations"
- Position it prominently (before or alongside existing cards)
- Add a summary metric to the top metrics grid: "Pending Invitations" showing count of SENT/OPENED invitations

**src/app/(protected)/advisor/settings/page.tsx:**
Create advisor settings page (or extend if it exists) with branding configuration:
- Section: "Branding"
  - Logo URL input field (text input for external URL, with https:// validation per research recommendation to start with URL, not file upload)
  - Preview of logo if URL is valid (small img tag with error fallback)
  - Save button that updates AdvisorProfile.logoUrl via a new server action
- Section: "Contact Information" (display-only, showing current profile info used in emails)
  - Show: name, email, phone, firm name, job title, license number
  - Link to update profile settings if they exist elsewhere

Create server action `updateAdvisorBranding(formData: FormData)`:
- Validate logoUrl is a valid https:// URL or empty string (to remove)
- Update AdvisorProfile.logoUrl
- Return success/error

Add "Settings" link to advisor navigation if not already present (check ProtectedNav or advisor layout).
  </action>
  <verify>Run `npx tsc --noEmit`. Navigate to /advisor to verify invitations card appears. Navigate to /advisor/settings to verify branding form renders.</verify>
  <done>Advisor portal has "Client Invitations" navigation card. Settings page allows logo URL configuration with preview. Pending invitation count shows in portal metrics.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete invitation flow end-to-end</name>
  <files>N/A</files>
  <action>
Human verification of the complete invitation system. No code changes - this is a manual walkthrough of the end-to-end flow.
  </action>
  <verify>
    1. Navigate to /advisor/invitations
    2. Fill out the invitation form with a test email, optional name, and personal message
    3. Submit - verify success toast appears and invitation shows in table with "Sent" status
    4. Check email (or Resend dashboard) - verify branded email with advisor logo, personal message, credentials
    5. Click the registration link in the email - verify signup form pre-fills email and name
    6. Complete registration - verify redirect to /intake
    7. Return to /advisor/invitations - verify invitation status changed to "Registered"
    8. Test resend: send another invitation, then click "Resend" - verify new email sent
    9. Test expire: click "Expire" on an invitation - verify status changes to "Expired"
    10. Navigate to /advisor/settings - verify logo URL can be saved and previewed
  </verify>
  <done>All 10 verification steps pass. Complete flow from invitation send through client registration works correctly with proper status tracking.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- Invitation form validates inputs and sends emails
- Invitation table displays status with colored badges
- Resend and expire actions work correctly
- Advisor portal has navigation to invitations page
- Settings page allows logo URL configuration
- Full flow: invite -> email -> register -> linked to advisor
</verification>

<success_criteria>
- Advisor can send, view, resend, and expire client invitations from the portal
- Invitation status is visible with clear visual indicators
- Advisor branding (logo URL) can be configured in settings
- Complete end-to-end flow verified by human tester
</success_criteria>

<output>
After completion, create `.planning/phases/015-secure-client-invitations/015-04-SUMMARY.md`
</output>
