---
phase: 14-family-dashboard
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - src/app/(protected)/family/dashboard/page.tsx
  - src/app/(protected)/family/dashboard/loading.tsx
  - src/components/family/HouseholdMemberList.tsx
  - src/components/family/FamilyScoreDisplay.tsx
  - src/components/family/ScoreTrendChart.tsx
  - src/components/family/EmphasisIndicator.tsx
autonomous: true

must_haves:
  truths:
    - "Family members can log in and see their governance score with household member names displayed"
    - "Families can view their score improvements over time when they complete multiple annual assessments"
    - "Family dashboard clearly shows which governance areas received extra attention from their advisor (1.5x emphasis)"
    - "Family members can view their risk pillar breakdown with category explanations"
    - "Advisors and admins are redirected away from family dashboard to their appropriate portal"
  artifacts:
    - path: "src/app/(protected)/family/dashboard/page.tsx"
      provides: "Family dashboard page with governance overview"
      contains: "FamilyDashboardPage"
    - path: "src/app/(protected)/family/dashboard/loading.tsx"
      provides: "Loading skeleton for family dashboard"
      contains: "export default"
    - path: "src/components/family/HouseholdMemberList.tsx"
      provides: "Household member names and roles display"
      contains: "HouseholdMemberList"
    - path: "src/components/family/FamilyScoreDisplay.tsx"
      provides: "Governance score with pillar breakdown and emphasis"
      contains: "FamilyScoreDisplay"
    - path: "src/components/family/ScoreTrendChart.tsx"
      provides: "Historical score trend line chart"
      contains: "ScoreTrendChart"
    - path: "src/components/family/EmphasisIndicator.tsx"
      provides: "Advisor emphasis visual indicator badge"
      contains: "EmphasisIndicator"
  key_links:
    - from: "src/app/(protected)/family/dashboard/page.tsx"
      to: "src/lib/family/queries.ts"
      via: "getFamilyDashboardData called in server component"
      pattern: "getFamilyDashboardData"
    - from: "src/components/family/FamilyScoreDisplay.tsx"
      to: "src/components/family/EmphasisIndicator.tsx"
      via: "renders EmphasisIndicator for each pillar score"
      pattern: "EmphasisIndicator"
    - from: "src/components/family/ScoreTrendChart.tsx"
      to: "recharts"
      via: "LineChart with ResponsiveContainer for historical trends"
      pattern: "LineChart"
---

<objective>
Build the family self-service dashboard page and all UI components including household member display, governance score with pillar breakdown, historical trend chart, and advisor emphasis indicators.

Purpose: Deliver the family-facing dashboard where households can view their governance progress, track improvements over time, and see advisor-customized emphasis areas.
Output: Fully functional /family/dashboard route with all four FAMILY requirements satisfied.
</objective>

<execution_context>
@/Users/bwoodtalton/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bwoodtalton/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-family-dashboard/14-RESEARCH.md
@.planning/phases/14-family-dashboard/14-01-SUMMARY.md

# Existing patterns to follow:
@src/app/(protected)/dashboard/page.tsx
@src/components/analytics/ChartContainer.tsx
@src/lib/analytics/formatters.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create family UI components</name>
  <files>src/components/family/HouseholdMemberList.tsx, src/components/family/EmphasisIndicator.tsx, src/components/family/FamilyScoreDisplay.tsx, src/components/family/ScoreTrendChart.tsx</files>
  <action>
Create four components in `src/components/family/`:

**HouseholdMemberList.tsx:**
- Props: `members: FamilyHouseholdMember[]` from family types
- Render Card with "Household Members" editorial-kicker heading
- For each member: show fullName, relationship (lowercase, replace underscores with spaces), and governance roles as Badge variant="outline"
- Return null if members array is empty
- Follow the Card + CardContent pattern from existing dashboard

**EmphasisIndicator.tsx:**
- Props: `pillarName: string, score: number, isEmphasized: boolean, className?: string`
- Show pillar name with score (X.X/10)
- If emphasized: render a Badge variant="secondary" with Shield icon and "Advisor Focus" text
- If emphasized: render Alert with "Your advisor gave this area extra attention in your assessment"
- Render Progress bar (value = score/10 * 100):
  - If emphasized: use warning color for bar and border highlight
  - If not emphasized: green >= 7.5, amber >= 5.0, orange >= 2.5, red < 2.5
- Import Shield from lucide-react, Badge from @/components/ui/badge, Alert/AlertDescription from @/components/ui/alert, Progress from @/components/ui/progress

**FamilyScoreDisplay.tsx:**
- Props: `currentScore: number, pillarScores: FamilyPillarScore[], advisorEmphasis: string[]`
- Show overall score prominently (text-5xl font-semibold, "X.X / 10")
- Show risk level badge based on score: >= 7.5 "Low Risk" green, >= 5.0 "Moderate Risk" amber, < 5.0 "High Risk" red
- Section heading "Governance Categories" below the score
- Render EmphasisIndicator for each pillar score, passing `isEmphasized: advisorEmphasis.includes(pillar.pillar)`
- Use a grid layout: 1 col mobile, 2 cols on md+

**ScoreTrendChart.tsx:**
- Mark as `"use client"` (Recharts requires client rendering)
- Props: `historicalAssessments: FamilyHistoricalAssessment[], advisorEmphasis: string[]`
- If fewer than 2 assessments: show message "Complete another annual assessment to see your improvement trends"
- Map assessments to chart data: date (format completedAt as "MMM yyyy" using formatChartDate), overallScore
- Use Recharts: ResponsiveContainer (width="100%", height={300}), LineChart, CartesianGrid, XAxis (dataKey="date"), YAxis (domain=[0,10]), Tooltip, Line (type="monotone", dataKey="overallScore", stroke primary blue, strokeWidth=3)
- Import formatChartDate from @/lib/analytics/formatters
  </action>
  <verify>Run `npx tsc --noEmit` — zero type errors. Verify all four component files exist.</verify>
  <done>Four family components created: HouseholdMemberList shows family names and roles, EmphasisIndicator shows advisor focus areas with visual highlighting, FamilyScoreDisplay shows overall score with pillar breakdown, ScoreTrendChart shows historical trends with Recharts.</done>
</task>

<task type="auto">
  <name>Task 2: Create family dashboard page and loading skeleton</name>
  <files>src/app/(protected)/family/dashboard/page.tsx, src/app/(protected)/family/dashboard/loading.tsx</files>
  <action>
**page.tsx** — Server Component following existing /dashboard/page.tsx pattern:
- Import auth from @/lib/auth, redirect from next/navigation
- Import getFamilyDashboardData from @/lib/family/queries
- Import all four family components
- Import Card, CardContent, CardHeader, CardTitle, CardDescription from @/components/ui/card

- Get session via auth(). If no session?.user?.id, redirect to /auth/signin
- Check role: if ADVISOR or ADMIN, redirect to /advisor (same pattern as /dashboard/page.tsx)
- Call getFamilyDashboardData(session.user.id)

- Layout structure (matching hero-surface pattern from existing dashboard):
  1. Hero section: "Family Dashboard" editorial-kicker, "Family Governance Progress" heading (text-3xl sm:text-5xl), description text, and HouseholdMemberList in the right column
  2. If currentScore exists: Card with FamilyScoreDisplay showing current score, pillar scores, and emphasis
  3. If hasMultipleAssessments: Card with "Your Progress Over Time" title and ScoreTrendChart
  4. If no assessments at all (currentScore is null): Empty state card with message "Complete your first governance assessment to see your results here" and Button linking to /assessment

- Use space-y-6 sm:space-y-8 layout matching existing dashboard

**loading.tsx** — Dashboard skeleton:
- Render animated skeleton placeholders matching page structure
- Hero section skeleton, score card skeleton, chart skeleton
- Use div with animate-pulse and bg-muted/50 rounded blocks
- Export as default function
  </action>
  <verify>Run `npx tsc --noEmit` — zero type errors. Run `npx next build` — build succeeds with /family/dashboard route registered. Verify both page.tsx and loading.tsx exist.</verify>
  <done>Family dashboard page renders at /family/dashboard with household member names, governance score with pillar breakdown, historical trend chart, and advisor emphasis indicators. Loading skeleton provides smooth loading experience. Advisors redirected to /advisor portal.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- `npx next build` succeeds with /family/dashboard route in build output
- Page imports and uses getFamilyDashboardData from Plan 01
- HouseholdMemberList displays family member names and governance roles (FAMILY-01)
- ScoreTrendChart shows historical score improvements (FAMILY-02)
- FamilyScoreDisplay renders pillar breakdown with category names (FAMILY-03)
- EmphasisIndicator visually highlights advisor focus areas (FAMILY-04)
- Role-based redirect prevents advisors from accessing family dashboard
</verification>

<success_criteria>
- FAMILY-01: Family members see governance score dashboard with household member names and roles
- FAMILY-02: Historical trend chart shows score improvements across multiple assessments
- FAMILY-03: Risk pillar breakdown displays all 8 governance categories with scores and explanations
- FAMILY-04: Advisor emphasis indicators clearly mark areas that received 1.5x focus with visual badges and highlighted progress bars
- Build passes with no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/14-family-dashboard/14-02-SUMMARY.md`
</output>
