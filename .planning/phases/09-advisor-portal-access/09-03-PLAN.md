---
phase: 09-advisor-portal-access
plan: 03
type: execute
wave: 3
depends_on: ["09-02"]
files_modified:
  - src/app/(protected)/advisor/layout.tsx
  - src/app/(protected)/advisor/page.tsx
  - src/components/advisor/ClientCard.tsx
  - src/components/advisor/NotificationBell.tsx
  - src/app/(protected)/layout.tsx
autonomous: true

must_haves:
  truths:
    - "Advisor sees a dashboard listing assigned clients with intake status"
    - "Non-advisor users are redirected away from /advisor routes"
    - "Each client card shows name, email, intake status, and review action"
    - "Notification bell shows unread count badge"
    - "Advisor navigation link appears for users with advisor role"
  artifacts:
    - path: "src/app/(protected)/advisor/layout.tsx"
      provides: "Advisor route layout with role gate"
      min_lines: 20
    - path: "src/app/(protected)/advisor/page.tsx"
      provides: "Advisor dashboard page with client list"
      min_lines: 50
    - path: "src/components/advisor/ClientCard.tsx"
      provides: "Client card component showing intake status and actions"
      min_lines: 40
    - path: "src/components/advisor/NotificationBell.tsx"
      provides: "Notification bell with unread count badge"
      min_lines: 25
  key_links:
    - from: "src/app/(protected)/advisor/page.tsx"
      to: "src/lib/actions/advisor-actions.ts"
      via: "Calls getAdvisorDashboardData server action"
      pattern: "getAdvisorDashboardData"
    - from: "src/app/(protected)/advisor/layout.tsx"
      to: "src/lib/auth.ts"
      via: "Checks session.user.role for ADVISOR or ADMIN"
      pattern: "role.*ADVISOR"
    - from: "src/components/advisor/ClientCard.tsx"
      to: "/advisor/review/[id]"
      via: "Link to intake review page"
      pattern: "href.*advisor/review"
---

<objective>
Build the advisor dashboard shell: protected layout with role gate, client list page, client cards, and notification indicator. This is the advisor's home base.

Purpose: Advisors need a dedicated portal separate from the client experience. The dashboard shows their assigned clients and intake review status at a glance.
Output: Advisor layout, dashboard page, client card and notification bell components.
</objective>

<execution_context>
@/Users/bwoodtalon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bwoodtalon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-advisor-portal-access/09-02-SUMMARY.md
@src/app/(protected)/layout.tsx
@src/lib/advisor/types.ts
@src/lib/actions/advisor-actions.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Advisor layout with role gate and dashboard page</name>
  <files>
    src/app/(protected)/advisor/layout.tsx
    src/app/(protected)/advisor/page.tsx
    src/app/(protected)/layout.tsx
  </files>
  <action>
1. **Create src/app/(protected)/advisor/layout.tsx**:
   - Server component. Call `auth()` to get session.
   - Check `session?.user?.role` is 'ADVISOR' or 'ADMIN'. If not, `redirect('/dashboard')` with a toast-friendly query param like `?error=unauthorized`.
   - Render children wrapped in a minimal container. No separate header -- uses parent protected layout header.
   - Add a subtle "Advisor Portal" badge/indicator at the top so advisors know they're in the advisor view.

2. **Create src/app/(protected)/advisor/page.tsx**:
   - Server component. Call `getAdvisorDashboardData()` server action.
   - Display advisor profile info (name, firm, specializations).
   - Show client count and unread notification count in summary cards.
   - Render list of assigned clients using ClientCard component (created in Task 2).
   - Empty state: "No clients assigned yet. Contact your administrator to get started."
   - Follow existing design patterns: hero-surface, editorial-kicker, section-divider classes.
   - Layout: Summary stats at top (2-3 metric cards), then client grid below.

3. **Update src/app/(protected)/layout.tsx**:
   - Add conditional "Advisor" nav link that only renders when `session?.user?.role === 'ADVISOR' || session?.user?.role === 'ADMIN'`.
   - Place after "Intake" and before "Assessment" in the nav grid.
   - Use same Button asChild variant="ghost" pattern as other nav links.
  </action>
  <verify>Run `npm run build` succeeds. Verify advisor layout checks role. Verify nav link is conditionally rendered based on role.</verify>
  <done>Advisor routes protected by role check. Dashboard page renders client list. Nav shows advisor link only for advisor/admin users.</done>
</task>

<task type="auto">
  <name>Task 2: ClientCard and NotificationBell components</name>
  <files>
    src/components/advisor/ClientCard.tsx
    src/components/advisor/NotificationBell.tsx
  </files>
  <action>
1. **Create src/components/advisor/ClientCard.tsx**:
   - Props: `AdvisorDashboardClient` type from advisor/types.ts.
   - Card layout showing: client name (heading), client email (muted text), assignment date.
   - Intake status section: If client has a latest interview, show status badge (color-coded: NOT_STARTED=gray, IN_PROGRESS=yellow, COMPLETED=blue, SUBMITTED=green). Show response count (e.g., "7/10 questions answered").
   - Action button: If interview status is SUBMITTED, show "Review Intake" button linking to `/advisor/review/${interview.id}`. If no interview or not submitted, show muted "Awaiting intake" text.
   - Use shadcn Card component, lucide-react icons (User, FileText, Clock).
   - Responsive: Cards in a grid (1 col mobile, 2 cols md, 3 cols lg).

2. **Create src/components/advisor/NotificationBell.tsx**:
   - Client component ("use client").
   - Props: `initialCount: number` (unread notification count from server).
   - Render a Bell icon (lucide-react) with a red badge showing count if > 0.
   - onClick: Navigate to `/advisor/notifications` (future page, just link for now).
   - Use relative positioning for badge overlay on bell icon.
   - Badge: small red circle with white text, absolute positioned top-right of bell.
  </action>
  <verify>Run `npx tsc --noEmit` passes. Run `npm run build` succeeds. Verify ClientCard renders status badges and review links. Verify NotificationBell shows count badge.</verify>
  <done>ClientCard displays client intake status with review action. NotificationBell shows unread count. Both follow existing design system patterns.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds without errors
- /advisor route redirects non-advisor users
- Dashboard displays client list from server action
- ClientCard shows intake status and review link for submitted intakes
- NotificationBell displays unread count badge
- Advisor nav link appears only for advisor/admin roles
</verification>

<success_criteria>
- Advisor dashboard accessible only to users with ADVISOR or ADMIN role
- Client cards show real data from assigned clients
- Navigation updated with conditional advisor link
- Empty state handled gracefully
- Design follows existing Belvedere aesthetic
</success_criteria>

<output>
After completion, create `.planning/phases/09-advisor-portal-access/09-03-SUMMARY.md`
</output>
