---
phase: 09-advisor-portal-access
plan: 04
type: execute
wave: 3
depends_on: ["09-02"]
files_modified:
  - src/app/(protected)/advisor/review/[id]/page.tsx
  - src/components/advisor/TranscriptViewer.tsx
  - src/components/advisor/AudioPlayer.tsx
  - src/components/advisor/RiskAreaSelector.tsx
  - src/components/advisor/ApprovalActions.tsx
autonomous: true

must_haves:
  truths:
    - "Advisor can read transcribed responses for each interview question"
    - "Advisor can play back original audio recordings"
    - "Advisor can select focus risk areas from assessment subcategories"
    - "Advisor can approve client for customized assessment"
    - "Approval transitions update status and store selected focus areas"
  artifacts:
    - path: "src/app/(protected)/advisor/review/[id]/page.tsx"
      provides: "Intake review page combining all review components"
      min_lines: 80
    - path: "src/components/advisor/TranscriptViewer.tsx"
      provides: "Scrollable transcript display for interview responses"
      min_lines: 60
    - path: "src/components/advisor/AudioPlayer.tsx"
      provides: "Audio playback controls for recorded responses"
      min_lines: 40
    - path: "src/components/advisor/RiskAreaSelector.tsx"
      provides: "Checkbox grid for selecting focus risk areas"
      min_lines: 50
    - path: "src/components/advisor/ApprovalActions.tsx"
      provides: "Approve/reject buttons with confirmation"
      min_lines: 40
  key_links:
    - from: "src/app/(protected)/advisor/review/[id]/page.tsx"
      to: "src/lib/actions/advisor-actions.ts"
      via: "Calls getIntakeReviewData and approval actions"
      pattern: "getIntakeReviewData"
    - from: "src/components/advisor/ApprovalActions.tsx"
      to: "src/lib/actions/advisor-actions.ts"
      via: "Calls approveClientIntake with selected focus areas"
      pattern: "approveClientIntake"
    - from: "src/components/advisor/RiskAreaSelector.tsx"
      to: "src/lib/advisor/types.ts"
      via: "Uses RISK_AREAS constant for checkbox options"
      pattern: "RISK_AREAS"
---

<objective>
Build the intake review page where advisors examine client interview responses, listen to audio recordings, select focus risk areas, and approve clients for customized assessment.

Purpose: This is the core advisor workflow -- reviewing qualitative intake data to determine which risk areas need deeper assessment. It fulfills ADVISOR-02, ADVISOR-03, ADVISOR-04, and ADVISOR-05.
Output: Review page with transcript viewer, audio player, risk area selector, and approval actions.
</objective>

<execution_context>
@/Users/bwoodtalon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bwoodtalon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-advisor-portal-access/09-02-SUMMARY.md
@src/lib/advisor/types.ts
@src/lib/actions/advisor-actions.ts
@src/lib/intake/questions.ts
@src/components/intake/AudioRecorder.tsx (reference for audio UI patterns)
</context>

<tasks>

<task type="auto">
  <name>Task 1: TranscriptViewer and AudioPlayer components</name>
  <files>
    src/components/advisor/TranscriptViewer.tsx
    src/components/advisor/AudioPlayer.tsx
  </files>
  <action>
1. **Create src/components/advisor/TranscriptViewer.tsx**:
   - Props: `responses: IntakeResponse[]`, `questions: IntakeQuestion[]`
   - Server-friendly component (no "use client" needed if purely display).
   - Render each question-response pair as a section:
     - Question number and text as heading (editorial-kicker + text-lg)
     - Question context as muted helper text
     - Transcription text in a readable block (bg-muted/50 rounded-lg p-4)
     - If transcription is empty/null, show "No transcription available" in italic muted text
     - If transcriptionStatus is FAILED, show small warning badge
     - AudioPlayer component inline for each response that has audioUrl
   - Map responses to questions using questionId matching against INTAKE_QUESTIONS
   - Scroll container with proper spacing between sections (space-y-8)
   - Show transcription confidence indicator if available (future-proof, optional display)

2. **Create src/components/advisor/AudioPlayer.tsx**:
   - "use client" component.
   - Props: `audioUrl: string`, `duration?: number`, `questionLabel?: string`
   - HTML5 audio element with custom styled controls (not native browser controls).
   - Play/Pause toggle button using lucide-react Play and Pause icons.
   - Progress bar: clickable/draggable range input showing current time / total duration.
   - Time display: current position in mm:ss / total duration in mm:ss.
   - Playback speed control: 0.75x, 1x, 1.25x, 1.5x (small button cycling through speeds).
   - Use useRef for audio element, useState for playing/currentTime/duration/playbackRate.
   - Handle onTimeUpdate, onLoadedMetadata, onEnded events.
   - Compact design: single row with play button, progress bar, time, speed control.
   - Error state: if audio fails to load, show "Audio unavailable" message.
  </action>
  <verify>Run `npx tsc --noEmit` passes. Verify TranscriptViewer maps questions to responses. Verify AudioPlayer has play/pause/seek/speed controls.</verify>
  <done>TranscriptViewer displays all interview responses with question context. AudioPlayer provides full playback controls for each recording.</done>
</task>

<task type="auto">
  <name>Task 2: RiskAreaSelector, ApprovalActions, and review page</name>
  <files>
    src/components/advisor/RiskAreaSelector.tsx
    src/components/advisor/ApprovalActions.tsx
    src/app/(protected)/advisor/review/[id]/page.tsx
  </files>
  <action>
1. **Create src/components/advisor/RiskAreaSelector.tsx**:
   - "use client" component.
   - Props: `selectedAreas: string[]`, `onChange: (areas: string[]) => void`, `disabled?: boolean`
   - Import RISK_AREAS from @/lib/advisor/types.
   - Render grid of checkboxes (2 cols on mobile, 4 cols on lg) with each risk area.
   - Each checkbox: area name as label, brief description below in muted text.
   - Selected areas highlighted with primary border/background.
   - Show count: "X of 8 areas selected" summary text.
   - Minimum 1 area required for approval (show validation message if none selected and form attempted).

2. **Create src/components/advisor/ApprovalActions.tsx**:
   - "use client" component.
   - Props: `interviewId: string`, `approvalId?: string`, `currentStatus?: string`, `selectedFocusAreas: string[]`, `notes: string`, `onStatusChange: () => void`
   - Three states based on currentStatus:
     - PENDING/null: Show "Begin Review" button (calls markIntakeInReview)
     - IN_REVIEW: Show "Approve for Assessment" (primary) and "Reject" (destructive) buttons. Approve button disabled if no focus areas selected. Both show confirmation dialog before action.
     - APPROVED: Show green "Approved" badge with approved date. Show "Revoke Approval" option (sets back to IN_REVIEW).
     - REJECTED: Show red "Rejected" badge with option to reopen.
   - Notes textarea: Allow advisor to add notes about their review.
   - Loading states on buttons during server action calls.
   - Use toast (react-hot-toast) for success/error feedback.
   - Call approveClientIntake with { interviewId, focusAreas, notes } on approve.
   - Call rejectClientIntake on reject.

3. **Create src/app/(protected)/advisor/review/[id]/page.tsx**:
   - Server component with async params (Next.js 15+ pattern: `params` is a Promise).
   - Call getIntakeReviewData(interviewId) server action.
   - If not found, show 404-style message with back link.
   - Layout (two-column on desktop, stacked on mobile):
     - Left/top: Client info header (name, email, submission date), then TranscriptViewer with all responses
     - Right/bottom (sticky sidebar on desktop): RiskAreaSelector and ApprovalActions
   - Client info header: name in heading, email in muted text, submitted date, response count, interview status badge.
   - Wrap RiskAreaSelector and ApprovalActions in a client component wrapper that manages shared state (selectedAreas, notes).
   - The wrapper component ("ReviewSidebar") should be "use client" and manage: selectedAreas state (initialized from existing approval.focusAreas if any), notes state (from approval.notes), and pass these to both child components.
   - Back link: "Back to Dashboard" using Link to /advisor.
  </action>
  <verify>Run `npm run build` succeeds. Verify review page loads intake data. Verify RiskAreaSelector renders all 8 areas. Verify ApprovalActions transitions through states.</verify>
  <done>Complete intake review page with transcript display, audio playback, risk area selection, and approval workflow. Advisor can review and approve clients for customized assessment.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds without errors
- Review page at /advisor/review/[id] displays interview data
- TranscriptViewer shows all question-response pairs
- AudioPlayer plays back recordings with controls
- RiskAreaSelector shows 8 assessment subcategories
- ApprovalActions transitions through PENDING -> IN_REVIEW -> APPROVED
- Focus areas stored with approval record
</verification>

<success_criteria>
- Advisor can view all transcribed responses mapped to questions
- Advisor can play back audio recordings with speed controls
- Advisor can select 1-8 focus risk areas from assessment subcategories
- Approval workflow persists status and focus areas
- Multi-tenant security: only assigned client intakes accessible
- Responsive layout works on desktop and tablet
</success_criteria>

<output>
After completion, create `.planning/phases/09-advisor-portal-access/09-04-SUMMARY.md`
</output>
