---
phase: 10-assessment-customization
plan: 04
type: execute
wave: 3
depends_on: ["10-02", "10-03"]
files_modified:
  - src/app/(protected)/assessment/results/page.tsx
autonomous: true
must_haves:
  truths:
    - "Results page shows customization context when assessment was advisor-customized"
    - "Standard results page unchanged for non-customized assessments"
  artifacts:
    - path: "src/app/(protected)/assessment/results/page.tsx"
      provides: "Results page with customization indicator in header"
  key_links:
    - from: "src/app/(protected)/assessment/results/page.tsx"
      to: "/api/assessment/[id]/score"
      via: "reads customization field from score response"
      pattern: "customization"
---

<objective>
Add customization context to the assessment results page so users see their results were advisor-guided.

Purpose: Completes the ASSESS-03 user experience — the customization indicator appears not just at the start (banner in hub) but also in the results, giving a complete advisor-guided experience.

Output: Modified results page with conditional customization display.
</objective>

<execution_context>
@/Users/bwoodtalton/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bwoodtalton/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-assessment-customization/10-02-SUMMARY.md
@.planning/phases/10-assessment-customization/10-03-SUMMARY.md
@src/app/(protected)/assessment/results/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add customization indicator to results page</name>
  <files>src/app/(protected)/assessment/results/page.tsx</files>
  <action>
Modify `src/app/(protected)/assessment/results/page.tsx` to display customization context:

1. Extend the `ScoreData` interface to include optional customization:
   ```typescript
   interface ScoreData {
     // existing fields...
     customization?: {
       isCustomized: boolean;
       focusAreaCount: number;
       emphasisMultiplier: number;
     };
   }
   ```

2. In the results header section (the `hero-surface` area), add a conditional display below the completion date:
   ```tsx
   {scoreData.customization?.isCustomized && (
     <div className="flex items-center gap-2 text-sm text-muted-foreground">
       <Shield className="h-4 w-4" />
       <span>
         Advisor-customized assessment focused on {scoreData.customization.focusAreaCount} risk {scoreData.customization.focusAreaCount === 1 ? 'area' : 'areas'}
       </span>
     </div>
   )}
   ```

3. Import `Shield` from lucide-react (or `UserCheck` — use whichever is already imported or simpler).

4. No other changes needed. The score breakdown already shows category-level scores which naturally reflect the emphasis weighting. The `ScoreDisplay`, `RiskDrivers`, and `ActionPlan` components receive data that already incorporates customization from the scoring engine.

IMPORTANT: This is a minimal change. Do NOT restructure the page or add new sections. Just add the indicator text.
  </action>
  <verify>Run `npx tsc --noEmit` — no type errors. Run `npm run build` — build succeeds.</verify>
  <done>Results page shows "Advisor-customized assessment" indicator when applicable. Standard results completely unchanged.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` — clean compilation
- `npm run build` — successful build
- Results page renders without errors
</verification>

<success_criteria>
- Customized assessment results show advisor context in header
- Standard assessment results page completely unchanged
- No new components needed — minimal inline addition
</success_criteria>

<output>
After completion, create `.planning/phases/10-assessment-customization/10-04-SUMMARY.md`
</output>
